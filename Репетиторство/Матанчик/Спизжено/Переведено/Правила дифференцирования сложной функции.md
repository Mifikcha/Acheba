Сложная функция – это функция, аргументом которой является другая функция.

f(g), где g-функция g(x)

Например:

x2-простая функция

Если мы заменим переменную этой функции на другую простую функцию, например на , тогда получим сложную функцию:

(sin x)2-сложная функция

Можем заменить и этот аргумент уже сложной функции на другую функцию, например, на 7х2. Получим:

(sin (7x))2 - сложная функция

Для того, чтобы найти производную такой сложной функции, мы должны взять производную на каждом этапе её «наслаивания» и перемножить получившиеся производные:

image_description

АЛГОРИТМ НАХОЖДЕНИЯ ПРОСТОЙ ФУНКЦИИ В СЛОЖНОЙ:

Рассмотрим алгоритм на примере сложной функции

image_description

1. Рассмотрим самую «внешнюю» функцию – это функция квадрата, т. к. вся сложная функция возведена во вторую степень. Мы знаем, как находится производная от квадрата:

image_description

Тогда в данном случае, вместо мы видим функцию image_description.

image_description

Тогда найдем производную функции по формуле дифференцирования квадрата, но так, чтобы вместо стояло выражение image_description:

image_description

Мы нашли производную первого «слоя» нашей сложной функции.

2. Если в предыдущем «слое» функции мы находили производную, меняя на выражение 7х, тогда найдем и его производную.

image_description

Мы знаем, как находится производная синуса:

image_description

Но в данном выражении x заменен на 7х, тогда при нахождении производной заменим и аргумент в формуле – х меняется на 7х:



Мы нашли производную второго «слоя» сложной функции.

3. Снова в предыдущем пункте мы меняли аргумент на выражение 7х.

image_description

Значит найдем производную и этого выражения тоже:

image_description

Это была простая линейная функция, поэтому заменять аргумент выражением не пришлось. Найдена производная последнего «слоя» нашей сложной функции.

4. Теперь, для нахождения общей сложной функции нам нужно перемножить полученные производные:



Пример № 1:

Найдите производную функции

image_description

1. Самой «внешней функцией является логарифм, т. к. остальные функции связаны с его аргументом. Производная логарифма находится по формуле:

image_description

Найдем производную внешней функции. Вместо обычного аргумента подставляем функцию х3:

image_description

2. Теперь найдем производную внутренней функции, т. е. функции, стоящей в аргументе внешней – х3:

image_description

3. Последняя функция является простой, значит нужно перемножить все получившиеся производные:

image_description