Существуют различные методы решения уравнений в зависимости от их вида. Но существуют такие методы решения, которые применимы ко всем из них. Данные методы в совокупности описывают общие принципы решения уравнений. Разберем каждый метод по отдельности.

МЕТОД ГРУППИРОВКИ:

Метод группировки предполагает разложить многочлен на множители, но предварительно объединить между собой попарно слагаемые.

Чтобы разложить многочлен на множители способом группировки, необходимо сделать следующее:

image_description

1. Записать друг за другом одночлены с одинаковыми буквенными (или иногда числовыми) множителями.

image_description

2. Вынести общий множитель за скобки у каждой группы одночленов.

image_description

3. Вынести полученный общий многочлен за скобки.

image_description

Пример №1:

Решите уравнение

image_description

1. Видим, что имеются функции разного типа – синус и косинус и аргумент в первом слагаемом 2x отличается от остальных в два раза. Перенесем все в левую часть:

image_description

2. По формуле синуса двойного угла преобразуем первое слагаемое для того, чтобы иметь одинаковые аргументы:

image_description

3. А теперь постараемся объединить пары и вынести общие множители в каждой скобке:

image_description

4. Заметим, что из первой скобки можно вынести image_description, а из второй image_description:

image_description

5. У нас получился общий многочлен, который мы снова вынесем за скобки и получим два множителя:

image_description

6. Далее стандартным способом решим данное уравнение, то есть приравняем сначала первую скобку к нулю, а затем и вторую:

image_description

7. Решим два уравнения и найдем корни:

image_description

**_Ответ:_** image_description

Таким образом, мы во время решения тригонометрического уравнения использовали способ группировки. Группировать слагаемые мы можем самым разнообразным способом: по парам, как мы это сделали в примере, по тройкам, используя формулы сокращенного умножения или просто вынесением за скобки. Группировкой можно пользоваться в самых разных типах уравнений.

МЕТОД ЗАМЕНЫ ПЕРЕМЕННОЙ:

1. Вводим новую переменную рационально, для упрощения уравнения. Для этого сначала нужно заметить повторяющийся элемент.

2. Во время замены необходимо указывать дополнительные условия для новой переменной, так как могут быть самые различные ограничения в зависимости от уравнения.

3. Далее мы получаем более простое уравнение, которое быстро решаем, и возвращаемся к исходной переменной.

4. В конце решаем еще раз уравнение относительно первоначальной переменной и получаем корни.

Пример №2:

Решите уравнение

image_description

1. Запишем ОДЗ. Всегда помним про этот шаг:

image_description

2. Замену вводить пока рано, так как во втором слагаемом аргументом является 2x, а не x, как в первом. Поэтому воспользуемся свойством суммы логарифмов и разложим логарифм из второго слагаемого на два:

image_description

3. Логарифм двух по основанию два это единица, значит, получим следующий вид:

image_description

4. Теперь у нас есть повторяющиеся элементы, это подсказка для введения замены. Но в целом, изначально мы могли обратить внимание на то, что уравнение похоже на квадратное, а значит, это сигнал на введение замены:

image_description

5. В данном случае никаких ограничений у нас нет. Запишем уравнение с новой переменной:

image_description

6. Решим его и получим значения.

image_description

7. Переходим к обратной замене.

image_description

8. И получаем корни.

image_description

**_Ответ:_** image_description

Бывает так, что можно заменить тригонометрическую функцию, например, косинус, тангенс или показательную функцию, если она фигурирует в уравнении несколько раз с одинаковым основанием. Более того можно заменять целые одинаковые выражения, они обычно в уравнениях стоят в скобках.

МЕТОД МОНОТОННОСТИ:

Метод монотонности используется, когда работа стандартными способами, такие как метод замены, разложение на множители и т. д., не приводят к успеху.

Данный метод предполагает представление выражений, которые стоят слева и справа в виде функций, и поиск точки их пересечения. Как правило, такая точка находится подбором, но этого мало, нужно показать, почему данная точка только одна. А показать мы это можем с помощью графиков или знаний о возрастании/убывании функций.

Пример №3.

Решите уравнение

image_description

1. Видим, что слева от знака равенства у нас показательная функция, а справа линейная:

image_description

Так как основание показательной функции больше единицы, то она возрастает, а у линейной функции коэффициент k отрицательный, значит, функция убывает.

2. Для большей наглядности можно изобразить данные два графика на координатной плоскости:

image_description

3. Для монотонно возрастающей и монотонно убывающей функций не может более одного пересечения, а значит, более одного решения уравнения. Работать будем подбором, так как в подобных заданиях всегда ответом является целое число, то начнем с единицы.

image_description

По графику видно, что абсциссой точки пересечения двух графиков является 1, что подтверждает найденный подбором ответ.

**_Ответ:_** 1.

ЗАМЕНА ОДНОГО УРАВНЕНИЯ ДРУГИМ:

Если мы имеем уравнение, обе части которого являются одинаковыми монотонными функциями, мы можем приравнять эти аргументы:

image_description

Этот переход является равносильным только если функция у = h (x) является монотонной. Если эта функция НЕ монотонна, тогда этот метод применять нельзя, т. к. в таком случае теряются корни.

1. Запишем ограничения исходной функции.

2. Исследуем функцию у = h (x) на монотонность.

3. Приравниваем функции image_description и image_description.

4. Решаем полученное уравнение.

Пример №4:

Решите уравнение

image_description

1. У корня третьей степени нет никаких ограничений, при этом подкоренные выражения тоже их не имеют.

2. Видим, что с обеих сторон у нас находятся кубические корни. Функция кубического корня является возрастающей, значит мы можем применить метод замены одного уравнения другим.

3. Приравняем аргументы функции кубического корня:

3x + 5 = x – 7

3. Решим полученное уравнение:

2x = –12

x = –6

**_Ответ:_** –6.