Для решения задач с параметром графическим методом нужно знать основные типы функций и внешний вид их графиков

Основные функции:

1. y = kx + b — линейная функция, графиком является прямая. В зависимости от коэффициента k может возрастать, убывать или проходить параллельно координатным осям.

image_description

2. y = ax2 + bx + c — квадратичная функция, графиком является парабола. В зависимости от коэффициента a ветви параболы направлены вверх или вниз.

image_description

3. image_description — функция обратной пропорциональности, графиком является гипербола. В зависимости от коэффициента k лежит в 1 и 3 или 2 и 4 четвертях.

image_description

4. y = logax — логарифмическая функция. В зависимости от основания a может возрастать или убывать.

image_description

5. y = ax — показательная функция. В зависимости от основания a может возрастать или убывать.

image_description

6. y = |f(x)| — функция модуля. Изначально строится график функции f(x), затем его отрицательная часть отражается в положительную.

image_description

7. (x – a)2 + (y – b)2 = R2 — уравнение окружности с центром в точке (a; b) и радиусом |R|.

image_description

image_description задает верхнюю полуокружность с центром в точке (a; b) и радиусом |R|.
image_description

image_description задает нижнюю полуокружность с центром в точке (a; b) и радиусом |R|.
image_description

8. |x – a| + |y – b| = c — уравнение квадрата с центром в точке (a; b). Если перед модулями коэффициенты отличаются, то графиком будет ромб.

image_description

Сдвиги функций

Сдвиг

Характеристика

fa(x) = f(x) + a

График отличается от графика функции f(x) смещением вдоль оси Оу на а единиц вверх при a > 0 и на а единиц вниз при a < 0.

fa(x) = f(x + a)

График отличается от графика функции f(x) смещением вдоль оси Оx на а единиц влево при a > 0 и на а единиц вправо при a < 0.

fa(x) = af(x)

График отличается от графика функции f(x) сжатием вдоль оси Оу при a > 1, расширением 0 < a < 1 и преобразованием симметрии относительно оси абсцисс при a = –1.


Решение уравнений и их систем графическим методом

Основные этапы решения задач с параметром графическим методом:

1. Приведение каждого уравнения системы к элементарной функции или разбиение уравнения на две элементарные функции;

2. Определение типов функций и построение графиков без параметра;

3. Сдвиги графиков с параметром в зависимости от значения параметра и анализ взаимного расположения;

4. Использование знаний алгебры или планиметрии, если не возможно по графику определить значение параметра а.

Пример:

Найдите все значения параметра a, при которых система image_description имеет ровно 1 решение.

Решение:

1) Рассмотрим каждое уравнение системы по-отдельности:

2. x2 + y2 = 9 — уравнение окружности с центром в точке (0; 0) и радиусом 3.

3. y = a — набор прямых, параллельных оси OX.

4) Построим графики функций в одной координатной плоскости:

image_description

3) Получаем, что ровно одно пересечение графики будут иметь при касании в точках (0; 3) и (0; –3). Тогда значение параметра равно 3 и –3.

**_Ответ:_** при a = 3 и a = –3.

Решение неравенств графическим методом

Основное отличие в решении неравенств и уравнений графическим методом заключается в том, что при работе с неравенствами на координатной плоскости строятся не просто графики (линии), а выделяются целые области.

Пример:

Найдите все значения параметра a, при которых система image_description имеет хотя бы одно решение на промежутке (2; 4).

Решение:

1) Рассмотрим каждое неравенство системы по-отдельности:

2. y = x + 2 — линейная функция, график — прямая.

y ≥ x + 2 — область координатной плоскости, лежащая выше прямой y = x + 2.

2. y = ax — пучок прямых, проходящих через точку (0; 0) и имеющих разные углы наклона.

y ≤ ax — область координатной плоскости, лежащая ниже прямой y = ax.

2) Построим данные области на координатной плоскости, а также выделим заданный в условии промежуток:

3. При a < 0

image_description

Заметим, что при отрицательных значениях параметра выделенные области пересекаются левее заданного промежутка. Значит данные значения нам не подходят.

2. При a = 0

image_description

Выделенные области пересекаются в точке (2; 0), но так как границы заданного промежутка исключены, то данный случай нам также не подходит.

3. При a > 0

image_description

Выделенные области пересекаются внутри заданного промежутка, поэтому система будет иметь хотя бы 1 решение.

Причем, при увеличении значения параметра a угол наклона прямой, ограничивающей область y ≤ ax, будет увеличиваться, а значит будет увеличиваться площадь пересечения y ≤ ax и y ≥ x + 2. Таким образом, при a > 0 система будет иметь хотя бы 1 решение.

**_Ответ:_** при a > 0.

Самые распространенные комбинации функций в заданиях с параметром

Чаще всего в задании № 17 встречаются комбинации различных функций с окружностью.

1) Взаимное расположение прямых и окружности

Прямая y = a может не пересекать окружность, пересекать в одной точке и пересекать в двух точках.
Пучок прямых y = ax + b имеет две общих точки с окружностью, если общая точка, принадлежащая пучку прямых, лежит внутри окружности.
image_description

Пучок прямых y = ax + b имеет две общих точки, одну точку или не имеет точек с окружностью, если общая точка, принадлежащая пучку прямых, лежит вне окружности.
image_description

2) Взаимное расположение двух окружностей

Окружности могут пересекаться (две общие точки), касаться (одна общая точка), совпадать (множество общих точек), и не пересекаться (не иметь общих точек).

Если d — расстояние между центрами двух окружностей, то:

d = R + r, если окружности касаются внешним образом;
d = R – r, если окружности касаются внутренним образом.