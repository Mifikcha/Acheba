Первообразная – это функция, от которой взяли производную.

Например, возьмём производную от функции





В данном примере  – это производная от image_description – первообразная от . То есть производная и первообразная – взаимообратные понятия.

image_description

Обратим внимание на свободный член в первообразной. Заменим его на любое другое число, например:

image_description

И найдем производную уже от этой функции:

image_description

Как мы видим, у одной и то же функции image_description может быть несколько первообразных:

image_description

Так получается, потому что определение производной связано с коэффициентом наклона функции, а свободный член влияет на перемещение функции вверх и вниз вдоль оси Oy. Поэтому бесконечное количество функций с одинаковым коэффициентом наклона, которые расположены параллельно друг другу, будут иметь одинаковые производные.

image_description

Это значит, что, если мы берем первообразную от функции, она будет не одна, их будет бесконечное множество. Единственное, чем будут отличаться эти первообразные – это свободный член (или константа) С:

image_description

где С – обычное число, константа.

ПРАВИЛА НАХОЖДЕНИЯ ПЕРВООБРАЗНОЙ:

Чтобы найти первообразную от данной функции, нужно использовать правила дифференцирования и применять их в обратном порядке. Нужно подумать, у какой функции нужно взять производную по правилам дифференцирования, чтобы получить данную функцию.

Помимо этого, есть еще три правила, которые нужно учитывать при нахождение первообразных.

ПРАВИЛО 1

Если нужно найти первообразную сумм двух функций, нужно найти их первообразные по отдельности, а потом сложить их.

image_description

Пример №1:

Найдите первообразную функции

image_description

1. Мы знаем, что image_description – это производная от image_description.

2. А вот примера 3х2 в таблице дифференцирования нет, потому что это частный случай степенной функции. Мы видим, что степень икса на единицу меньше, чем коэффициент перед ним, значит первообразной этого слагаемого была функция х3.

3. Запишем первообразную для функции image_description, главное – не забыть константу

image_description

Проверка:

Проверить себя очень просто – возьмём производную от полученной первообразной:

image_description

Значит первообразная найдена верно.

Ответ: image_description.

ПРАВИЛО 2

Если для всей функции можно вынести общий коэффициент, то можно найти первообразную оставшейся функции и умножить её на этот коэффициент.

image_description

Пример №2:

Найдите одну из первообразных функции

image_description

1. Вынесем общий множитель 4 за скобку:

image_description

2. Теперь найдем первообразные для скобки. Так как это сумма двух функций, найдем их производные по правилу 1.

Первообразная от image_description – это image_description, а первообразная от (-sin x) – это cos x.

3. Запишем первообразную для image_description с учётом коэффициента. Константу здесь добавлять необязательно, т. к. нужно найти только одну из первообразных:

image_description

Проверка:

Возьмем производную от полученной первообразной:

image_description

Ответ: image_description.

ПРАВИЛО 3

Для функции вида image_description первообразной является функция image_description.

Пример №3:

Найдите одну из первообразных функции

image_description

1. Видим, что внутри функции у = cos x вместо икса стоит линейное выражение 2х + 3. По третьему правилу нужно найти первообразную так, чтобы её аргументом было это выражение 2х + 3, а потом разделить его на коэффициент при х, в данном случае – на 2:

image_description

Проверка:

Возьмем производную от получившейся первообразной. Данная функция является сложной:

image_description

Первообразная найдена верно, запишем ответ.

Ответ: image_description.