Модуль числа а – это расстояние (в единичных отрезках) от начала координат до точки А с координатой a.

Модуль числа а обозначается как.

Рассмотрим координатную прямую:

image_description

1. Найдем модуль числа 3. Чтобы это сделать, нужно посчитать, чему равно расстояние от начала координат до точки с координатой 3 (например А (3)):

image_description

Значит модуль числа 3 равен трём единичным отрезкам:

image_description

2. Найдем модуль числа –3. Так же посчитаем расстояние от начала координат, но уже до точки с координатой –3 (например В(–3)):

image_description

Таким образом модуль числа –3 тоже равен трём единичным отрезкам:

image_description

Так как модуль – это расстояние, то он не может быть отрицательным, и модули противоположных чисел равны:

image_description

3. Если мы попробуем найти модуль числа 0, то увидим, что от точки с координатой 0 до начала координат нет расстояния, т.к. это одна и та же точка, значит:

image_description

Исходя из предыдущих пунктов можно выделить одно общие правила для модуля:

1. Для положительного числа и нуля модуль равен самому числу.

2. Для отрицательного числа модуль равен противоположному числу.

3. Модули противоположных чисел равны.

Итак, можно сказать, что image_description, если число неотрицательное, и image_description, если число отрицательное. По-другому модуль называется абсолютной величиной числа.