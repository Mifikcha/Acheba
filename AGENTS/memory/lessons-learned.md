# Память рабочей папки Codex

## Журнал задач

| ID | Дата | Статус | Цель | Артефакты | Заметки |
| :-- | :-- | :-- | :-- | :-- | :-- |
| CX-001 | 2026-06-03 | DONE | Установить memory-skill из `google-gemini/gemini-cli` | AGENTS/memory/SKILL.md | Оригинальный Gemini-specific скилл был скачан из GitHub-источника. |
| CX-002 | 2026-06-03 | DONE | Адаптировать memory-skill под Codex | AGENTS/memory/SKILL.md | Пути и ритуалы Gemini Bot заменены на правила памяти Codex в этой рабочей папке. |
| CX-003 | 2026-06-03 | DONE | Переписать memory-skill на русский | AGENTS/memory/SKILL.md, AGENTS/memory/lessons-learned.md | Добавлено устойчивое правило: в этой папке писать на русском. |
| CX-004 | 2026-06-04 | DONE | Установить physics-solver skill из `beita6969/ScienceClaw` | AGENTS/physics-solver/SKILL.md | Скилл адаптирован под Codex и переписан на русский; удалён чужой локальный venv-путь. |
| CX-005 | 2026-07-12 | DONE | Добавить properties к заметкам по физике | репетиторство/шизончик/теория | В 32 физические заметки добавлен YAML frontmatter для учебной программы: `type`, `topic_id`, `subject`, `section`, `title`, `order`, длительность, зависимости, статус, видимость и готовность материалов. |
| CX-006 | 2026-07-12 | DONE | Собрать базы и программы по физике для 9 и 11 класса | Репетиторство/Шизончик/Базы | `.base` файлы перенесены в папку `Базы`; добавлены Markdown-программы групп по физике для 9 и 11 класса. |
| CX-007 | 2026-07-12 | DONE | Зафиксировать правила заполнения properties учебных заметок | AGENTS/memory/lessons-learned.md | Добавлены правила для `curriculum_topic`: статусы, порядок, видимость, готовность материалов и запрет на `level`/`difficulty`. |
| CX-008 | 2026-08-03 | DONE | Пересобрать главную Quartz под vault-лендинг и Tokyo Night | `Публичный сайт/index.md`, `_quartz/quartz/styles/custom.scss`, `_quartz/quartz.config.yaml`, `_quartz/quartz/components/scripts/sidebar-collapse.inline.ts` | Главная получила крупный hero, быстрые ссылки на `Шизончик`/`Инфушечка`/`Матанчик`, встроенный широкий Quartz graph, заготовки ресурсов/отзывов/контактов; панели по умолчанию закрыты. |
| CX-009 | 2026-08-03 | DONE | Убрать правую боковую панель Quartz | `_quartz/quartz.config.yaml`, `_quartz/quartz/styles/custom.scss`, `_quartz/quartz/components/scripts/sidebar-collapse.inline.ts` | Для content-страниц `positions.right` очищен, правая плавающая стрелка удалена, пустая правая колонка Quartz скрывается через CSS; встроенный граф на главной сохранён. |
| CX-010 | 2026-08-03 | DONE | Добить левый зазор Quartz и KaTeX в `№ 18 Параметр` | `_quartz/quartz/components/Head.tsx`, `Публичный сайт/.../№ 18 Параметр/*.md` | Поздний inline CSS `data-hopes-layout-fix` ставит `#quartz-root`, `#quartz-body` и `.left.sidebar` от `x=0`; в трех файлах параметра исправлены display math блоки, после сборки `katex-error` в целевой папке не найден. |

## Журнал решений

- **2026-06-03**: Использовать `AGENTS/About_me.md` как устойчивый профиль пользователя в этой рабочей папке.
- **2026-06-03**: Использовать `AGENTS/memory/lessons-learned.md` как журнал задач, решений, фактов проекта, правил папки и follow-up пунктов для Codex.
- **2026-06-03**: По умолчанию писать все правки, заметки, планы, пояснения и memory-файлы в этой рабочей папке на русском языке.
- **2026-06-20**: Для папки `хуйнюшка` при разборе статей по ссылкам делать `.md`-выжимки с чуть более широким контекстом: в каждом смысловом разделе добавлять на 2-3 предложения больше пояснений из статьи, а не только сухую таблицу или факт.
- **2026-06-20**: Для закрытых/частично закрытых источников в `хуйнюшка` явно помечать уровень доступа: ResearchGate может отдавать 403, Springer/ScienceDirect иногда доступны только как preview/abstract; в таких случаях фиксировать DOI, издательскую страницу, аннотацию, метаданные и ограничения, не выдавая это за полный текст.
- **2026-07-12**: Для физических тем в `репетиторство/шизончик/теория` использовать YAML properties по схеме `curriculum_topic`; `topic_id` задаётся в формате `PHYS-...`, а материалы остаются в текущих разделах без пересортировки.
- **2026-07-12**: Базы Obsidian и Markdown-программы по физике хранить в `Репетиторство/Шизончик/Базы`; программы ссылками указывают на заметки из `Репетиторство/Шизончик/Теория`.
- **2026-07-12**: В учебных заметках не использовать properties `level` и `difficulty`; разделение по классам и экзаменам делать через `.base` файлы и программы, а не через поле уровня в каждой теме.

## Факты проекта

- `AGENTS/About_me.md` содержит актуальный профиль пользователя; читать его перед персональной стратегией, планированием и задачами, чувствительными к предпочтениям.
- `AGENTS/memory/SKILL.md` адаптирован под Codex и не должен ссылаться на Gemini-specific runtime paths вроде `tools/gemini-cli-bot/lessons-learned.md`.
- `AGENTS/physics-solver/SKILL.md` содержит русскоязычный Codex-адаптированный навык для решения физических задач, выводов формул, моделирования и перевода единиц.
- Физические заметки в `репетиторство/шизончик/теория` размечены как темы учебной программы через YAML frontmatter; текущие разделы: `reference`, `mechanics`, `molecular_physics`, `electrodynamics`, `quantum_physics`.
- В `Репетиторство/Шизончик/Базы` лежат базы и программы: `База по физике за 9 класс.base`, `База по физике за 11 класс.base`, `Физика 9 класс — программа группы.md`, `Физика 11 класс — программа группы.md`.

## Правила рабочей папки

- Рабочий язык этой папки — русский.
- Все правки, редактирование, заметки, планы, пояснения и memory-файлы по умолчанию писать на русском, если пользователь явно не попросил другой язык.
- Для исследовательских выжимек в `хуйнюшка` сохранять не только извлечённые данные, но и контекст: зачем показатель нужен автору, как он связан с аргументом статьи, какие ограничения/источники данных указаны, и короткий интерпретирующий вывод.

### Правила properties для учебных заметок

- Для тем учебной программы использовать `type: curriculum_topic`.
- Для физических тем использовать `subject: physics`; для будущих предметов задавать аналогично коротким латинским id (`math`, `informatics`), если пользователь не задаст другой стандарт.
- `topic_id` — стабильный машинный код темы. Для физики формат `PHYS-SECTION-NNN`, например `PHYS-MECH-001`. Не менять `topic_id` при переименовании файла без явного запроса.
- `section` — короткий латинский id раздела. Для физики текущие значения: `reference`, `mechanics`, `molecular_physics`, `electrodynamics`, `quantum_physics`.
- `title` — человекочитаемое название темы на русском, обычно совпадает с названием заметки без `.md`.
- `order` — последовательный номер темы внутри своего `section`: `1`, `2`, `3` и далее без шагов по 10. При добавлении темы в середину раздела перенумеровать порядок внутри раздела, если пользователь не попросит оставить зазоры.
- `duration_lessons` — примерная длительность темы в занятиях; ставить осторожную оценку целым числом, а при сомнении оставлять прежнее значение или `1`.
- `prerequisites` и `next_topics` — YAML-списки `topic_id`. Если зависимости не проработаны, оставлять `[]`, не выдумывать связи.
- `status` описывает готовность заметки как элемента учебной программы: `draft` — черновик/не вычитано; `ready` — можно использовать в программе; `needs_review` — надо пересмотреть; `archived` — устарело и не использовать. По умолчанию для новых или неоценённых материалов ставить `draft`.
- `student_visible` — можно ли показывать ученику. По умолчанию `true` для обычных теоретических тем; `false` для внутренних методических заметок, черновых планов, личных комментариев и служебных файлов.
- `parent_visible` — можно ли показывать родителю. По умолчанию `true` для нейтральных тем программы; `false` для внутренних комментариев, оценок слабых мест конкретных учеников и методических заметок.
- `materials_ready` описывает готовность материалов: `missing` — материалов нет; `partial` — есть основа, но не всё готово; `complete` — тема полностью собрана и проверена. По умолчанию ставить `partial`, если текст заметки уже есть, но качество не проверялось.
- Не добавлять `level` и `difficulty` в учебные темы. Для отбора тем по 9/11 классу, ОГЭ/ЕГЭ и группам использовать отдельные `.base` файлы и Markdown-программы.

### Google Sheets sync для физики

- **2026-07-13**: Для интеграции Obsidian -> Google Sheets добавлен слой `tools/google_sheets`: `sync_to_google_sheets.py`, `README.md`, `.env.example`, `requirements.txt`, `OBSIDIAN_SCHEMA.md`.
- Obsidian остается первичным источником истины; Google Sheets/CSV являются операционной витриной. Поток для будущего Telegram-бота: бот -> подтверждение -> запись в Obsidian -> синхронизация Sheets.
- Локальный CSV-экспорт создается в `Репетиторство/Шизончик/Google Sheets Export`; текущая физика дает 2 программы, 32 темы и 6 legacy-учеников-кандидатов из папок статистики.
- **2026-08-02**: Главная Quartz в `Публичный сайт/index.md` оформлена как строгая точка входа в базу с тремя карточками: `Шизончик` (физика), `Матанчик` (математика), `Инфушечка` (информатика). Стили лежат в `_quartz/quartz/styles/custom.scss`; сетка карточек адаптивная, чтобы в узкой центральной области Quartz карточки не сжимались слишком сильно. Стандартные `article-title`/`content-meta` на главной скрыты через CSS. Панели Quartz управляются кастомным `sidebar-collapse.inline.ts`: кнопка книги в панели скрыта, левый проводник и правая панель графа/обратных ссылок схлопываются краевыми стрелками, состояние хранится в `localStorage`. При переключении панелей скрипт вызывает `render`, чтобы граф не оставался с нулевой шириной после раскрытия. Граф дополнительно усилен настройками в `_quartz/quartz.config.yaml` и стилями в `custom.scss`. Страницы папок/подразделов оформлены тем же `custom.scss`: селектор `body[data-slug$="/index"]:not([data-slug="index"])` делает книжную шапку, компактный счетчик элементов и минималистичные строки-карточки для вложенных разделов.
- **2026-08-02**: Правая панель Quartz оставлена только с графом: плагин `@quartz-community/backlinks` выключен в `_quartz/quartz.config.yaml`. Граф сделан кучнее через меньшие `repelForce`/`linkDistance`, больший `centerForce` и `focusOnHover: false`; размер точек, контраст связей, толщина линий и минимальный перезапуск физики при зажатой точке (`alphaTarget(0.03)`) патчатся скриптом `_quartz/scripts/patch-graph.mjs`, который запускается в `postinstall` и перед `build`, потому что эти параметры не вынесены наружу плагином `@quartz-community/graph`.
- **2026-08-02**: Для эффекта LaTeX/научной статьи сайт Quartz использует `Libertinus Serif` для заголовков, текста и интерфейса, а `JetBrains Mono` для кода. Шрифты заданы и в `configuration.theme.typography`, и в опциях плагина `@quartz-community/quartz-fonts` в `_quartz/quartz.config.yaml`, иначе плагин добавляет старые дефолтные Google Fonts (`Schibsted Grotesk`, `Source Sans Pro`, `IBM Plex Mono`) поверх основной темы.
- **2026-08-02**: Футер Quartz оставляет подпись `Создано с помощью Quartz v5.0.0`, а ссылка `Telegram` в `@quartz-community/footer` ведет на профиль пользователя `https://t.me/Skifcha` через `_quartz/quartz.config.yaml`.
- **2026-08-03**: Главная Quartz теперь оформлена ближе к `ssp.sh/brain/data-engineering/`: первый экран с крупным `Hopes and Dreams`, затем три плотные ссылки-раздела, ниже широкая интерактивная плашка графа, затем пустой блок ключевых ресурсов, отзывы и контакт Telegram. Цвета сайта переведены на палитру Tokyo Night из Obsidian-темы `tcmmichaelb139/obsidian-tokyonight`; кастомные токены лежат в `_quartz/quartz/styles/custom.scss`, базовые Quartz colors — в `_quartz/quartz.config.yaml`.
- **2026-08-03**: Правая боковая панель Quartz пока не используется: для content-страниц в `_quartz/quartz.config.yaml` задано `positions.right: []`, правая кнопка управления удалена из `sidebar-collapse.inline.ts`, а пустой контейнер `.right.sidebar:empty` скрывается в `custom.scss`. Граф остаётся встроенным вручную на главной через HTML-блок `.home-graph`.
- **2026-08-03**: Текущий пресет встроенного графа на главной: `scale: 0.22` для локального контейнера и `0.225` для глобального, `repelForce: 0.18`, `centerForce: 0.95`, `linkDistance: 84`, `opacityScale: 1.35`; в общем Quartz-конфиге `localGraph` держит `scale: 0.26`, `centerForce: 0.95`, `opacityScale: 2.05`. `scale` работает как стартовый zoom через патч `_quartz/scripts/patch-graph.mjs`; без этого патча он почти не отдаляет граф и может раздувать подписи. Не поднимать `centerForce` выше `1`: у d3 force center это нестабильный диапазон.
- **2026-08-03**: Tokyo Night для Quartz нужно форсировать не только через `_quartz/quartz.config.yaml`, но и через CSS-переменные в `_quartz/quartz/styles/custom.scss` с `!important` на `:root`, `:root[saved-theme="dark"]` и `:root[saved-theme="light"]`, потому что базовые стили Quartz могут перебивать палитру темы. Карточки главной должны перекрывать стандартный фон `a.internal` фиолетовым `--secondary`.
- **2026-08-03**: Пустой зазор слева от открытой левой панели создаёт тема Quartz, а не grid: `@quartz-themes/core` генерирует `resource-style-*` с `.page:not([data-frame=canvas]):not([data-frame=excalidraw]) { padding-left: calc(50% - min(1500px, 100dvw) / 2); ... }`. CSS override в `custom.scss` и патч `@quartz-themes/core/dist/index.js` через `_quartz/scripts/patch-graph.mjs` полезны, но финальный надёжный слой — поздний inline CSS в `_quartz/quartz/components/Head.tsx` после `additionalHead`: стиль `data-hopes-layout-fix` перебивает `#quartz-root`, `#quartz-body` и `.left.sidebar` через `!important`. Практическая проверка после сборки на viewport 1920×900: при открытой левой панели `#quartz-root.x = 0`, `#quartz-body.x = 0`, `.left.sidebar.x = 0`, `.left.sidebar.width = 320`, `.right.sidebar.display = none`.
- **2026-08-03**: В Markdown для Quartz/KaTeX не смешивать display math с blockquote в виде `>$$\begin{cases}` и не писать `$$\begin{cases} ... \end{cases}$$` одной рамкой на строках с содержимым. Для систем и больших выводов использовать отдельные строки `$$`, `\begin{cases}`/`\begin{aligned}` и закрывающий `$$`; иначе после одной ошибки KaTeX может подсветить красным весь следующий текст.

## Открытые follow-up

- Нет.
