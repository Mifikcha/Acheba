Параметр – это буквенный коэффициент в уравнении.

Параметр – это всегда число, а не переменная, но мы не знаем, чему конкретно равен параметр. Например:

image_description

это уравнение прямой, в котором x – переменная, y – зависимая от неё функция, а k и b – коэффициенты. Это значит, что k и b – какие-то числа, параметры. Когда мы видим конкретное уравнение прямой, например:

image_description

мы можем сказать, что в данном случае параметр k = -5, а параметр b = 8. В зависимости от параметров функция может по-разному себя вести, но сам вид линейной функции не поменяется.

УРАВНЕНИЕ С ПАРАМЕТРОМ:

Существуют уравнения, где есть две неизвестных: x – корень уравнения и a (или любая другая буква) – параметр. Решение таких уравнений сводится не к поиску конкретных корней, а к анализу их количества. Для этого мы предполагаем, чему будут равны корни уравнения при определенных параметрах.

АЛГОРИТМ РЕШЕНИЯ УРАВНЕНИЙ С ПАРАМЕТРОМ

image_description

Рассмотрим примеры.

Пример №1:

Сколько корней имеет уравнение

image_description

1. Данное уравнение не имеет ограничений для x, поэтому перейдем ко второму пункту.

2. Начнем рассуждать. В случае работы с параметром нужно предположить, как мы будем искать корень, если коэффициент при икс равен нулю и если не равен ему. В данном случае коэффициент при иксе и есть параметр. Допустим, a = 0, тогда уравнение будет иметь вид:

0 = 7

Это невозможно, из чего делаем вывод, что при a = 0 корней нет.

3. Теперь представим, что параметр не равен нулю, а равен любому другому числу image_description, тогда выразим икс:

image_description

При условии, что image_description, а равно обычному числу, x будет принимать одно единственное значение.

4. Так мы нашли, при каких параметрах уравнение будет иметь один корень, нужно проверить этот корень на ограничения. Этих ограничений из п.1 нет, значит мы полностью проанализировали уравнение и узнали, сколько корней оно будет иметь во всех возможных случаях изменения параметра:

корней нет при a = 0

один корень image_description при image_description

Ответ так и запишем.

Ответ: корней нет при a = 0; один корень image_description при image_description

При работе с линейными уравнениями нет ограничений для переменных и для параметра. Сейчас мы рассмотрим дробно-рациональное уравнение с параметром, где на каждом этапе нужно помнить об ограничениях в знаменателе.

Пример №2:

Сколько корней имеет уравнение

image_description

1. Для начала нужно выписать все ограничения для переменной. Знаменатель не может быть равен нулю, значит:

image_description

2. Теперь нам нужно преобразовать наше уравнение и сделать его линейным. Пока, если мы приравняем параметр к нулю, не получим конкретных результатов. Приведем уравнение к общему знаменателю:

image_description

Ограничение для переменной мы записали, поэтому смело можно убрать знаменатель и приравнять числители. Про ограничение не забывать!

image_description

3. Вправо перенесем все с иксами, а влево перенесем все остальные числа:

image_description

4. Мы снова пришли к ситуации, когда х умножается на какое-то число (4 + k), значение которого мы не знаем, т. к. в нём есть параметр. Снова анализируем количество корней, если весь коэффициент перед x равен нулю, то есть:

image_description

Тогда

image_description

Чего не может быть, значит корней нет.

5. Рассмотрим случай, когда image_description:

image_description

тогда

image_description 

Получается, что существует один единственный корень.

6. Мы не учли то, что единственным корнем может оказаться число 3, но в начале мы записали ограничение image_description. Поэтому уравнение будет иметь один корень image_description при image_description, если

image_description

Найдем такой параметр , при котором x = 3. Нужно проверить, есть ли такой параметр, и если есть – исключить его:

image_description

Что невозможно, значит такого параметра не существует, и уравнение имеет ровно один корень без ограничений, кроме image_description. Запишем ответ.

Ответ: корней нет при image_description; один корень image_description при image_description.