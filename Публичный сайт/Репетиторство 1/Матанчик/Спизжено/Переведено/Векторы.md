ОСНОВНЫЕ ПОНЯТИЯ И ОБОЗНАЧЕНИЯ:

Вектор – отрезок, у которого есть начало и конец, то есть указано его направление.

Вектор обозначается через точки начала и конца вектора, например image_description (первая буква – начало вектора, вторая – конец) или, если мы хотим обозначить вектор без указания точек, пишем просто image_description.

Точка тоже может быть вектором, в таком случае вектор называют нулевым, т.к. его началом и концом является одна и та же точка. Обозначаем нулевой вектор как, например, image_description или image_description.

image_description

Длина или модуль вектора – это длина отрезка (как если бы у него не было направления). Длина вектора image_description обозначается как image_description, длина вектора image_description как image_description, а длина нулевого вектора всегда равна нулю:

image_description

РАСПОЛОЖЕНИЕ ВЕКТОРОВ:

Коллинеарные векторы – это векторы, которые лежат на параллельных прямых или на одной прямой. Нулевой вектор коллинеарен любому вектору.

Сонаправленные векторы – это коллинеарные векторы, которые направлены в одну сторону.

Обозначается как image_description (вектор image_description сонаправлен вектору image_description)

Противоположно направленные векторы – это коллинеарные векторы, которые направлены в противоположные стороны.

Обозначаются как image_description (вектор image_description противоположно направлен вектору image_description)

Равные векторы – это сонаправленные векторы, у которых равны длины, т. е. у них одинаковые и направление, и длина.

Например:

image_description

1. image_description и image_description – коллинеарны, при этом противоположно направлены, т. к. лежат параллельных прямых и направлены в разные стороны:

image_description

2. image_description и image_description – коллинеарны, при этом сонаправлены, т .к. лежат параллельных прямых и направлены в одну сторону:

image_description

3. image_description и image_description – равны, т. к. сонаправлены (из п.2) и равны по модулю:

image_description

3. image_description коллинеарен всем векторам, и может являться им как сонаправленным, так и противоположно направленным, т. к. image_description.

СВОЙСТВА НЕНУЛЕВЫХ КОЛЛИНЕАРНЫХ ВЕКТОРОВ:

image_description

ОТКЛАДЫВАЕНИЕ ВЕКТОРА ОТ ТОЧКИ:

Говорят, что вектор отложен от точки, если она является его началом. Например, image_description отложен от точки А, image_description отложен от точки С и так далее. Можно откладывать абсолютно любые векторы абсолютно из любых точек. Это описывается следующим правилом:

От любой точки можно отложить вектор, равный данному и при том только один.

Например, возьмем точку М и два вектора image_description и image_description. Мы можем отложить от точки М вектора, равные image_description и image_description всего один раз. Делается это параллельным переносом:

image_description

Таким образом image_description, при этом image_description отложены от точки М.

СЛОЖЕНИЕ ВЕКТОРОВ:

При сложении векторов нужно учитывать их направления, поэтому проще всего складывать вектора визуально. Существуют два самых простых способа сложить два вектора – это правило треугольника и правило параллелограмма.

ПРАВИЛО ТРЕУГОЛЬНИКА

Если нужно найти сумму двух векторов, по правилу треугольника нужно:

1. Параллельным переносом перенести начало одного вектора в конец другого.

2. Пусть эти векторы будут сторонами треугольника, тогда третья его сторона – их сумма.

3. Обозначить направление получившегося вектора суммы – от начала первого вектора в конец второго (стрелка к стрелке).

Пример № 1:

Найдите сумму векторов image_description и image_description по правилу треугольника:

image_description

1. Перенесем вектор image_description так, чтобы он начинался там, где заканчивается вектор image_description.

image_description

2. Соединим эти векторы в треугольник, третьей стороной которой будет вектор image_description.

image_description

3. Направление вектора image_description будет идти от начала image_description до конца image_description (стрелка к стрелке):

ПРАВИЛО ПАРАЛЛЕЛОГРАММА:

Если нужно найти сумму двух векторов, по правилу параллелограмма нужно:

1. Параллельным переносом перенести начала этих векторов в одну точку.

2. Пусть эти векторы будут сторонами параллелограмма, тогда диагональ этого параллелограмма – их сумма.

3. Обозначить направление получившегося вектора суммы – от начала векторов в противоположный конец параллелограмма (по диагонали).

Пример № 2:

Найдите сумму векторов image_description и image_description по правилу параллелограмма:

image_description

1. Перенесем оба вектора параллельным переносом так, чтобы они начинались из одной точки.

image_description

2. Представим, что они являются сторонами параллелограмма.

image_description

3. Диагональ этого параллелограмма, которая начинается в точке начала векторов image_description и image_description – это вектор image_description.

image_description

СВОЙСТВА СЛОЖЕНИЯ ВЕКТОРОВ:

1. Переместительное свойство:

image_description

2. Сочетательное свойство:

image_description

СЛОЖЕНИЕ НЕСКОЛЬКИХ ВЕКТОРОВ:

Чтобы сложить несколько векторов, нужно ставить их друг за другом, сохраняя их направление (используя параллельный перенос), тогда их суммой будет являться вектор, начала которого – это начало первого вектора, а конец – конец последнего вектора (как в правиле треугольника).

Пример № 3:

Найдите сумму векторов image_descriptionи image_description:

image_description

1. Поставим эти векторы как бы по порядку сохраняя их длину и направление. По переместительному свойству неважно, в каком порядке мы будем располагать вектора. Соединим их, например, в таком порядке - image_description.

2. Проведем вектор их суммы от начала первого вектора в конец второго:

image_description

ВЫЧИТАНИЕ ВЕКТОРОВ:

Вычесть вектор – это то же самое, что прибавить отрицательный вектор:

image_description

То есть и при вычитании можно использовать правила сложения. Главное – найти противоположный вектор.

Само словосочетание «противоположный вектор» говорит о том, что такие вектора направлены в разные стороны. Значит вычесть вектор – значит прибавить вектор с противоположным ему направлением.

Мы можем проверить это свойство алгебраически. Мы знаем, что противоположные числа в сумме дают 0:

a + (–a) = a – a = 0

Тогда и сумма противоположных векторов дадут 0 (т. е. если мы «пойдем» от начала до конца image_description и обратно по – image_description, то мы вернемся снова в начало image_description):

image_description

Значит и для векторов справедливо это свойство:

image_description

Пример № 4:

Найдите image_description, если

image_description

1. Можем использовать сложение векторов, если мы найдем отрицательные векторы. В данном случае отрицательны векторы image_description и image_description. Тогда image_description, следующие:

image_description

2. Теперь сложим все векторы, учитывая отрицательные:

image_description

ПРОИЗВЕДЕНИЕ ВЕКТОРА И ЧИСЛА:

Произведением ненулевого вектора и числа является вектор, коллинеарный данному, длина которого равна произведению длины данного вектора и числа:

image_description

где k – это число, при этом:

image_description при k > 0

image_description при k < 0

Произведением любого вектора на ноль является нулевой вектор.

Пример № 5:

Найдите 5image_description и –2image_description , если:

image_description

1. Также можно представить произведение вектора и числа как сложение этого вектора несколько раз:

image_description

2. Аналогично поступим и с отрицательным числом, только теперь уже складываем противоположные векторы:

image_description

СВОЙСТВА ПРОИЗВЕДЕНИЯ ВЕКТОРА И ЧИСЛА:

1. Сочетательное свойство:

image_description

где k и l – некоторые числа

2. Распределительный закон:

image_description

и

image_description

ПРИМЕНЕНИЕ ВЕКТОРОВ К РЕШЕНИЮ ЗАДАЧ:

Используя векторы и связанные с ними свойства можно решать различные геометрические задачи.

Пример № 6:

Точка С – середина отрезка АВ, О – произвольная точка на плоскости. Докажите, что

image_description

1. По правилу треугольника:

image_description

2. Сложим два этих выражения, получим:

image_description

3. При этом image_description и image_description – противоположные векторы, т.к. равны по модулю (точка С середина АВ), и имеют противоположное направление, значит:

image_description

Что и требовалось доказать

image_description


Пример № 7:

ABCD – трапеция. Точки M и N середины оснований BC и AD соответственно. Точка О – точка пересечения прямых AB и CD. Докажите, что О, M и N лежат на одной прямой.

1. Треугольники OAD и OBC подобны по двум углам:

image_description

Значит их стороны относятся друг к другу с определенным отношением:

image_description

image_description

2. При этом соответствующие стороны коллинеарны, значит можем выразить их как произведение числа и вектора:

image_description

3. В данной задаче можем выразить image_description и image_description как

image_description

т.к. М – середина BC, а N – середина AD (аналогично вектору image_description из Примера №6).

4. Соединим выразим вектор image_description через image_description и image_description из пункта 3:

image_description

5. Если вектор image_description можно представить как произведение числа k с вектором image_description, значит image_description и image_description коллинеарны, а значит лежат на одной прямой (они не могут быть параллельны, т.к. уже пересекаются в точке О).

Что и требовалось доказать