Лемма – это вспомогательная теорема, с помощью которой доказывается более сложная и комплексная теорема.

РАЗЛОЖЕНИЕ ВЕКТОРОВ ПО ДВУМ НЕКОЛЛИНЕАРНЫМ ВЕКТОРАМ:

ЛЕММА О КОЛЛИНЕАРНЫХ ВЕКТОРАХ:

Если векторы image_description и image_description коллинеарны и image_description, то существует такое число k, что image_description

Эта лемма связана со свойствами умножения вектора на число. С помощью нее доказывается следующая теорема.

ТЕОРЕМА О РАЗЛОЖЕНИИ ВЕКОРОВ:

На плоскости любой вектор можно разложить по двум данным неколлинеарным векторам, причем с единственными коэффициентами разложения.

Разложить вектор – значит представить его в виде суммы векторов, каждый из которых является произведением вектора на коэффициент (по лемме о коллинеарных векторах):

image_description

где k и l – единственные подходящие коэффициенты

Например:

Рассмотрим три вектора: image_description, image_description, и image_description.

image_description

1. Расположим векторы так, будто вектор image_description находится по правилу параллелограмма:

image_description

2. Проведем через конец вектора image_description две прямые, параллельные векторам image_description и image_description

image_description

3. Чтобы image_description являлся суммой векторов, правило параллелограмма должно выполняться верно. Тогда продлим вектора image_description и image_description до пересечения с прямыми b и a соответственно:

image_description

4. Действительно, сумма векторов image_description и image_description по правилу параллелограмма равна вектору image_description:

image_description

image_description

Если бы коэффициенты k и l были равны единице, то image_description, то есть просто сумме векторов.

Таким образом можно разложить любой вектор по любым заданным неколлинеарным векторам.

КООРДИНАТЫ ВЕКТОРА:

Используя разложение векторов, можно связать любой вектор с координатной плоскостью. Мы знаем, что координатная плоскость образуется двумя перпендикулярными осями, у которых есть направление и единичные отрезки. Длина и направление – две характеристики любого вектора. Рассмотрим на каждой оси единичные векторы.

Пусть вектор image_description сонаправлен с осью Ох, а вектор image_description сонаправлен с осью Оу, при этом image_description.

Получается, что длины этих векторов задают единичные отрезки, а их направления задают направления осей:

image_description

Таким образом любой вектор на координатной плоскости можно разложить по единичным векторам image_description и image_description:

image_description

где x и y – координаты вектора image_description

Пример №1:

Разложите по единичным векторам и найдите координаты следующих векторов:

image_description

1. Проще всего раскладывать векторы, начало которых находится в точке (0; 0). Рассмотрим вектор image_description.

Для того, чтобы представить его как сумму единичных векторов, нужно посчитать, сколько отложили векторов image_description и image_description, чтобы получить они в сумме дали вектор image_description. Количество отложенных векторов image_description будет равнять координате x, а количество отложенных image_description – координате y:

image_description

Для получения вектора image_description мы сложили 4 вектора image_description и 2 вектора image_description, то есть:

image_description

В таком случае говорят, что вектор image_description имеет координаты {4; 2} и записывают это как:

image_description

2. Найдем координаты вектора image_description аналогично вектору image_description. В случае, когда начало вектора находится не в начале координат, будем отсчитывать единичные векторы от его начала:

image_description

image_description

3. Аналогично разложим вектор image_description и найдем его координаты:

image_description

image_description

Мы видим, что координаты векторов image_description и image_description равны. Действительно, если мы перенесем параллельным переносом вектор image_description в начало координат – точку, откуда начинается вектор image_description, то эти векторы совпадут. А значит векторы с одинаковыми координатами равны.

ДЕЙСТВИЯ С КООРДИНАТАМИ ВЕКТОРОВ:

1. Координаты суммы векторов:

а) Если существуют векторы с координатами:

image_description

И есть такой вектор image_description, что:

image_description

б) Тогда:

image_description

image_description

В) Значит координаты image_description равны:

image_description

Следовательно каждая координата суммы векторов равна сумме их соответствующих координат.

2. Координаты разности векторов:

а) Если существуют векторы с координатами:

image_description

И есть такой вектор image_description, что:

image_description

б) Тогда:

image_description

image_description

в) Значит координаты image_description равны:

image_description

Следовательно каждая координата разности векторов равна разности их соответствующих координат.

3. Координаты произведения вектора на число:

а) Если существует вектор

и число k

И есть вектор:

image_description

б) Тогда:

image_description

в) Значит координаты равны:

image_description

Следовательно каждая координата произведения вектора на число равна произведению соответствующей координаты на это число.

КООРДИНАТЫ НАЧАЛА И КОНЦА ВЕКТОРА:

При нахождении координат векторов в примере №1 мы использовали визуальный метод. Также, чтобы находить координаты вектора, можно использовать координаты его начала и конца.

Координаты каждого вектора равны ((х1 – х2); (у1 – у2)), где начало вектора имеет координаты (х1; у1), а конец координаты (х2; у2).

Пример №2:

Найдите координаты вектора image_description, если известны координаты точек А(0; 0) В(3; 4).

1. Для начала используем визуализацию, чтобы понять, где находится наш вектор:

image_description

2. Чтобы найти координату x данного вектора, посмотрим, сколько единичных векторов image_description «прошел вектор» от точки А до точки В:

image_description

Получается, что мы нашли разность между координатами x конечной и начальной точки вектора.

3. Аналогично найдем и координату y:

image_description

Она также равна разнице координат y конца и начала вектора.

4. Получается, координаты вектора image_description равны:

image_description

ФОРМУЛЫ, СВЯЗАННЫЕ С ВЕКТОРАМИ:

1. Координаты середины отрезка:

image_description

где (х1; у1) – координаты начала отрезка,

(х2; у2) – координаты конца отрезка,

(х; у) – координаты середины отрезка.

2. Длина вектора:

image_description

Где (х; у) – координаты вектора image_description

3. Расстояние между двумя точками:

image_description

где (х1; у1) – координаты точки А,

(х2; у2) – координаты точки В.

УРАВНЕНИЯ ПРЯМОЙ И ОКРУЖНОСТИ:

Используя координатной метод можно вывести уравнения, которые задают расположение прямой и окружности на координатной плоскости.

Уравнением прямой является уравнение первой степени:

image_description

где a, b и c – некоторые числа, а x и y – координаты на координатной плоскости

Уравнением окружности радиуса r с центром в точке (x0; y0) имеет вид:



Частным случаем является уравнение окружности с радиусом r и центром в начале координат:

