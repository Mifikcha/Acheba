Интеграл – это одно из важнейших понятий в математическом анализе. В общем смысле интеграл – это сумма всех первообразных функции. Существуют разные интегралы, которые применяются для вычисления площадей фигур и объёмов тел. В рамках школьного курса начала математического анализа изучают определенный интеграл, он равен площади криволинейной трапеции.

Криволинейной трапеция – это трапеция на координатной плоскости, с вертикальными основаниями x = a и x = b (где а и b – границы интегрирования), ограниченной сверху графиком функции image_description:

image_description

S(x) – площадь криволинейной трапеции

Определённый интеграл – это разность первообразных непрерывной неотрицательной функции в крайних точках заданного отрезка.

image_description

Интеграл называется определенным, потому что мы знаем, на каком промежутке [a;b] нужно находить первообразные.

Запись интеграла читается как «интеграл функции image_description от a до b». Буквосочетание говорит о том, что функция дифференцирована (т. е. является производной от искомой первообразной) относительно переменной x. Так как в высшей математике функции могут состоять из нескольких переменных, необходимо указывать, относительно какой именно переменной находилась производная

image_description

Пример №1:

Вычислите площадь фигуры, ограниченной сверху функцией

image_description

на промежутке [1; 4].

1. Площадь фигуры под функцией равна определенному интегралу, взятому на данной промежутке. Запишем этот интеграл:

image_description

2. Чтобы вычислить этот интеграл, нужно найти общую первообразную для данной функции, а потом найти ее значения в крайних точках промежутка. Первообразной от image_description является:

image_description

Когда после записи интеграла вы нашли общую первообразную, запись интеграла упрощается: справа от первообразной за вертикальной чертой обозначаются промежутки интегрирования снизу вверх, а dx больше не пишется:

image_description

3. Теперь нужно подставить в первообразную x = 4 и x = 1, между получившимися значениями будет знак «минус»:

image_description

4. Мы пришли к разнице первообразных. Осталось посчитать результат и записать ответ:

image_description

Ответ: 3.

Пример №2:

Вычислите площадь фигуры, ограниченной сверху функцией

image_description

на промежутке [-2; 1].

1. Запишем определенный интеграл:

image_description

2. У нас есть несколько слагаемых в функции. По правилам работы с первообразными их общей перообразной будет сумма их первообразных:

image_description

3. Подставим первообразную в формулу интеграла:

image_description

4. Посчитаем первообразные в крайних точках интегрирования и найдем площадь под функцией:



Ответ: 12

ГЕОМЕТРИЧЕСКИЙ СМЫСЛ ИНТЕГРАЛА:

Определенный интеграла и площадь под графиком функций связаны между собой через понятие приращения, как, например, связана производная и угол наклона касательной. Дело в том, что если мы поделим координатную плоскость под графиком бесконечным количеством вертикальных прямых, расстояние между которыми будет равно Δx, при этом image_description, тогда мы увидим, что вся площадь под графиком поделилась на бесконечное множество очень-очень узких прямоугольников, с одинаковой шириной Δx, но разной высотой.

image_description

Две верхние вершины такого прямоугольника будут находиться настолько близко, что сойдутся почти в одной точке. Первообразная функции в этой точке будет равна площади этого прямоугольника. А если мы сложим все такие прямоугольники под функцией, как раз найдем площадь криволинейной трапеции.