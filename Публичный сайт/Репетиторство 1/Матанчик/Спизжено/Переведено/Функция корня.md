Функция корня – это функция вида image_description где а ≠ 0; х ≥ 0 при k ˃ 0; x ≤ 0 при k < 0.

Графиком этой функции является перевернутая ветвь параболы.

image_description

В этом виде функции a = 1, k = 1.

С изменением коэффициента a меняется внешний вид функции.

ХАРАКТЕРИСТИКИ ФУНКЦИИ КОРНЯ:

В ЗАВИСИМОСТИ ОТ КОЭФФИЦИЕНТА a (при k>0)

image_description

В ЗАВИСИМОСТИ ОТ КОЭФФИЦИЕНТА k (при a > 0)

image_description

ВЛИЯНИЕ КОЭФФИЦИЕНТОВ НА ВИД ФУНКЦИИ КОРНЯ:

Коэффициент a

– При увеличении коэффициента a по модулю функция приближается к оси Оу.

– При уменьшении коэффициента a по модулю функция приближается к оси Ох.

image_description

– При a > 0 график находится в I четверти (y ≥ 0).

– При a < 0 график находится в IV четверти (y ≤ 0).

image_description

Коэффициент k

– При k > 0 график находится в l четверти (x ≥ 0).

– При k < 0 график находится во ll четверти (x ≤ 0).

image_description