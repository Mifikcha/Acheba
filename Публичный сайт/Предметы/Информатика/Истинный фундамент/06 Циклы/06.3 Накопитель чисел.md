---
title: 06.3 Накопитель чисел
tags:
  - coding_lesson
coding_order: 3
coding_task: "Напиши multiply_numbers_from_range(start, finish): верни произведение всех чисел от start до finish включительно."
starter_code: |-
  def multiply_numbers_from_range(start, finish):
      result = 1
      i = start
      # допиши цикл
      return result

  print(multiply_numbers_from_range(1, 5))
  print(multiply_numbers_from_range(2, 3))
tests: |-
  assert __output__.splitlines() == ["120", "6"]
  assert "while" in __source__
  assert "result" in __source__
  assert __stderr__ == ""
---

# Накопитель чисел

Накопитель — это переменная, которая собирает ответ по кусочкам.

Для суммы стартуют с `0`:

```python
total = 0
i = 1
while i <= 3:
    total = total + i
    i += 1
```

Для произведения стартуют с `1`, потому что умножение на `1` ничего не портит.

```python
result = 1
result = result * 2
result = result * 3
```

Если не помнишь, почему результат можно хранить в переменной, смотри [[03.9 Выражения в переменных]].

### Задачи

1. Посчитай сумму чисел от `1` до `n`.
2. Посчитай произведение чисел от `start` до `finish`.
3. Посчитай сумму только четных чисел от `1` до `n`.

### Предскажи вывод

```python
total = 0
i = 2
while i <= 4:
    total += i
    i += 1

print(total)
```

> [!tip]- Ответ
> Будет `9`.
