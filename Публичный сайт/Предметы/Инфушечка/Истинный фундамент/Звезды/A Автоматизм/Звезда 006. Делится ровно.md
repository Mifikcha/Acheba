---
title: Звезда 006. Делится ровно
tags:
  - coding_lesson
coding_order: 6
coding_scope: предметы/инфушечка/истинный-фундамент/звезды
coding_task: |-
  Напиши `divides(a, b)`, возвращающую `True`, если `a` делится на `b` без остатка. `b != 0`.
  
  Подсказка: Нужен `%`.
  
  Подсказка: Логическое условие можно вернуть сразу.
starter_code: |-
  def divides(a, b):
      # Напиши решение здесь
      pass
---

# Звезда 006. Делится ровно

Напиши `divides(a, b)`, возвращающую `True`, если `a` делится на `b` без остатка. `b != 0`.

> [!tip]- Подсказка 1
> Нужен `%`.

> [!tip]- Подсказка 2
> Логическое условие можно вернуть сразу.
