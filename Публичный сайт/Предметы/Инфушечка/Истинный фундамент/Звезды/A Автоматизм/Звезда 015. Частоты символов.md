---
title: Звезда 015. Частоты символов
tags:
  - coding_lesson
coding_order: 15
coding_scope: предметы/инфушечка/истинный-фундамент/звезды
coding_task: |-
  Верни словарь `символ -> количество появлений` для строки.
  
  Подсказка: Нужен словарь-счётчик.
  
  Подсказка: `.get(ch, 0)` даёт 0 для нового ключа.
starter_code: |-
  def frequencies(text):
      # Напиши решение здесь
      pass
---

# Звезда 015. Частоты символов

Верни словарь `символ -> количество появлений` для строки.

> [!tip]- Подсказка 1
> Нужен словарь-счётчик.

> [!tip]- Подсказка 2
> `.get(ch, 0)` даёт 0 для нового ключа.
