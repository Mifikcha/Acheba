---
title: Звезда 007. Тип символа
tags:
  - coding_lesson
coding_order: 7
coding_scope: предметы/инфушечка/истинный-фундамент/звезды
coding_task: |-
  Напиши `char_type(ch)`: `digit`, `letter` или `other`.
  
  Подсказка: У строк есть методы `isdigit()` и `isalpha()`.
  
  Подсказка: Используй `if / elif / else`.
starter_code: |-
  def char_type(ch):
      # Напиши решение здесь
      pass
---

# Звезда 007. Тип символа

Напиши `char_type(ch)`: `digit`, `letter` или `other`.

> [!tip]- Подсказка 1
> У строк есть методы `isdigit()` и `isalpha()`.

> [!tip]- Подсказка 2
> Используй `if / elif / else`.
