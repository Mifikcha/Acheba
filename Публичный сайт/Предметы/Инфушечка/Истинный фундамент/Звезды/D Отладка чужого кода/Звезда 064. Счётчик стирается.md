---
title: Звезда 064. Счётчик стирается
tags:
  - coding_lesson
coding_order: 64
coding_scope: предметы/инфушечка/истинный-фундамент/звезды
coding_task: |-
  Почему код возвращает максимум 1?
  
  Подсказка: Где должен создаваться накопитель?
  
  Подсказка: Не обнуляй его на каждой итерации.
starter_code: |-
  def count_a(text):
      # Напиши решение здесь
      pass
---

# Звезда 064. Счётчик стирается

Почему код возвращает максимум 1?

```python
def count_a(text):
    for ch in text:
        count = 0
        if ch == 'a':
            count += 1
    return count
```

> [!tip]- Подсказка 1
> Где должен создаваться накопитель?

> [!tip]- Подсказка 2
> Не обнуляй его на каждой итерации.
