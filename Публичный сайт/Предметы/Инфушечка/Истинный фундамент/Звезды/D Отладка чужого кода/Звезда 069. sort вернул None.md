---
title: Звезда 069. sort вернул None
tags:
  - coding_lesson
coding_order: 69
coding_scope: предметы/инфушечка/истинный-фундамент/звезды
coding_task: |-
  Почему `result = numbers.sort()` даёт `None`? Исправь двумя способами.
  
  Подсказка: `.sort()` меняет список на месте.
  
  Подсказка: `sorted()` возвращает новый список.
starter_code: |-
  # Исправь или напиши код здесь.
---

# Звезда 069. sort вернул None

Почему `result = numbers.sort()` даёт `None`? Исправь двумя способами.

> [!tip]- Подсказка 1
> `.sort()` меняет список на месте.

> [!tip]- Подсказка 2
> `sorted()` возвращает новый список.
