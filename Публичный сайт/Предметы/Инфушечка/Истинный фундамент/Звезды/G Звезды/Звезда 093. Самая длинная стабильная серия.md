---
title: Звезда 093. Самая длинная стабильная серия
tags:
  - coding_lesson
coding_order: 93
coding_scope: предметы/инфушечка/истинный-фундамент/звезды
coding_task: |-
  Для списка результатов и `tolerance` верни длину самой длинной серии, где разница между каждыми соседними результатами не больше `tolerance`.
  
  Подсказка: Серия проверяется по соседним парам.
  
  Подсказка: При нарушении начинай новую серию с текущего элемента.
starter_code: |-
  def stable_run(scores, tolerance):
      # Напиши решение здесь
      pass
tests: |-
  assert stable_run([1, 2, 3, 10], 1) == 3
  assert stable_run([5, 5, 5], 0) == 3
  assert stable_run([1, 4, 7], 2) == 1
---

# Звезда 093. Самая длинная стабильная серия

Для списка результатов и `tolerance` верни длину самой длинной серии, где разница между каждыми соседними результатами не больше `tolerance`.

Функция должна именно вернуть результат, а не печатать его. Сохраняй имя функции и порядок аргументов из условия: проверка будет вызывать её напрямую.

## Примеры

- `stable_run([10, 11, 15], 1)` → `2`
- `stable_run([], 2)` → `0`
- `stable_run([1, 5, 6], 1)` → `2`

> [!tip]- Подсказка 1
> Серия проверяется по соседним парам.

> [!tip]- Подсказка 2
> При нарушении начинай новую серию с текущего элемента.
