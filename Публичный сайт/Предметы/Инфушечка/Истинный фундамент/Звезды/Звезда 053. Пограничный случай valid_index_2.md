---
title: Звезда 053. Пограничный случай valid_index_2
tags:
  - coding_lesson
coding_order: 53
coding_task: "Напиши valid_index_2(text, index): True, если index неотрицательный и входит в строку."
starter_code: |-
  def valid_index_2(text, index):
      return None
  
  print(valid_index_2("abc", 2))
tests: |-
  assert valid_index_2("abc", 2) is True
  assert valid_index_2("abc", -1) is False
  assert __stderr__ == ""
---

# Звезда 053. Пограничный случай valid_index_2

Уровень: C

### Задача

Напиши valid_index_2(text, index): True, если index неотрицательный и входит в строку.