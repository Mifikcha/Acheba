---
title: Звезда 085. Маска каждого k-го
tags:
  - coding_lesson
coding_order: 85
coding_scope: предметы/инфушечка/истинный-фундамент/звезды
coding_task: |-
  `mask(text, step)` заменяет каждый `step`-й символ на `*`, считая позиции с 1.
  
  Подсказка: Позиция = `i + 1`.
  
  Подсказка: Каждый шаг добавляй либо `*`, либо исходный символ.
starter_code: |-
  def mask(text, step):
      # Напиши решение здесь
      pass
---

# Звезда 085. Маска каждого k-го

`mask(text, step)` заменяет каждый `step`-й символ на `*`, считая позиции с 1.

> [!tip]- Подсказка 1
> Позиция = `i + 1`.

> [!tip]- Подсказка 2
> Каждый шаг добавляй либо `*`, либо исходный символ.
