---
title: 05.2 Логический тип
aliases:
  - bool
tags:
  - coding_lesson
coding_order: 2
coding_task: "Создай переменную result со значением 12 % 5 == 2 и выведи result."
starter_code: |-
  result = False
  print(result)
tests: |-
  assert __output__.strip() == "True"
  assert "12 % 5 == 2" in __source__ or "12%5==2" in __source__
  assert __stderr__ == ""
---

# Логический тип

`True` и `False` — значения логического типа `bool`.

```python
result = 5 > 4
print(result)  # True
```

Сравнение — это выражение. Его результат можно сохранить в переменную, вернуть из функции или передать в `print`.

```python
def is_adult(age):
    return age >= 18
```

Если типы данных пока расплываются, смотри [[Типы данных]].

### Задачи

1. Сохрани в `answer` результат `100 % 10 == 0` и выведи.
2. Создай функцию `is_positive(number)`, которая возвращает `number > 0`.
3. Выведи `type(True)`.

### Предскажи вывод

```python
print(type(3 > 2))
```

> [!tip]- Ответ
> Будет `<class 'bool'>`.
