---
title: 05.3 Предикаты
aliases:
  - Предикаты
tags:
  - coding_lesson
coding_order: 3
coding_task: "Создай функцию is_mister(text), которая возвращает True только для строки Mister."
starter_code: |-
  def is_mister(text):
      return False

  print(is_mister("Mister"))
  print(is_mister("Missis"))
tests: |-
  assert __output__.splitlines() == ["True", "False"]
  assert "text == \"Mister\"" in __source__ or "text == 'Mister'" in __source__
  assert __stderr__ == ""
---

# Предикаты

Предикат — функция-вопрос. Она возвращает `True` или `False`.

```python
def is_even(number):
    return number % 2 == 0
```

Имена предикатов часто начинаются с `is_` или `has_`:

```python
is_even
is_empty
has_errors
```

Так код читается как обычный вопрос.

### Задачи

1. Создай `is_zero(number)`, которая проверяет `number == 0`.
2. Создай `is_python(text)`, которая проверяет `text == "Python"`.
3. Создай `has_plus(phone)`, которая проверяет первый символ номера.

### Предскажи вывод

```python
def is_big(n):
    return n > 100

print(is_big(150))
```

> [!tip]- Ответ
> Будет `True`.
