---
title: 05.7 Отрицание not
tags:
  - coding_lesson
coding_order: 7
coding_task: "Создай is_not_even(number) через not и уже готовую is_even(number)."
starter_code: |-
  def is_even(number):
      return number % 2 == 0

  def is_not_even(number):
      return False

  print(is_not_even(10))
  print(is_not_even(11))
tests: |-
  assert __output__.splitlines() == ["False", "True"]
  assert "not is_even(number)" in __source__
  assert __stderr__ == ""
---

# Отрицание not

`not` переворачивает логическое значение.

```python
print(not True)   # False
print(not False)  # True
```

Если уже есть предикат, часто не нужна новая сложная проверка:

```python
def is_even(number):
    return number % 2 == 0

def is_not_even(number):
    return not is_even(number)
```

Двойное отрицание возвращает исходную правду:

```python
not not True  # True
```

### Задачи

1. Создай `is_not_zero(number)`.
2. Создай `is_not_python(text)` через `not`.
3. Проверь `not (10 > 5)`.

### Предскажи вывод

```python
print(not (3 > 5))
```

> [!tip]- Ответ
> Будет `True`.
