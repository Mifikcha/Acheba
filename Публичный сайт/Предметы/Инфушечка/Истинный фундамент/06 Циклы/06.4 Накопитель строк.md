---
title: 06.4 Накопитель строк
tags:
  - coding_lesson
coding_order: 4
coding_task: "Напиши join_numbers_from_range(start, finish): верни строку из всех чисел диапазона подряд."
starter_code: |-
  def join_numbers_from_range(start, finish):
      result = ""
      i = start
      # допиши цикл
      return result

  print(join_numbers_from_range(2, 5))
tests: |-
  assert __output__.strip() == "2345"
  assert "while" in __source__
  assert "str(" in __source__
  assert __stderr__ == ""
---

# Накопитель строк

Строку тоже можно собирать постепенно.

```python
result = ""
i = 1
while i <= 3:
    result = result + str(i)
    i += 1

print(result)  # 123
```

Пустая строка `""` — удобное начальное значение для сборки текста.

Число нельзя напрямую склеить со строкой, поэтому используется `str(i)`. Если это место мутное, смотри [[03.1 Строка это текст]] и [[03.4 Склеивание строк]].

### Задачи

1. Собери строку `"12345"` циклом.
2. Собери строку из чисел диапазона `start..finish`.
3. Собери строку из одинакового символа `times` раз.

### Мини-проверка

```python
result = ""
i = 3
while i <= 5:
    result += str(i)
    i += 1

print(result)
```

> [!tip]- Ответ
> Будет `345`.
