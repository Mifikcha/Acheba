---
title: "№5 Алгоритмы"
tags:
  - егэ/задание-5
  - предмет/информатика
  - публичный-сайт
  - тип/разбор-задач
  - экзамен/егэ
course: ege-informatics
module: ege-tasks
ege:
  - 5
status: published
---
Перед разбором: [[Предметы/Инфушечка/Истинный фундамент/02 Минимальный Python/02.1 Первая программа|минимальный Питон]], [[Предметы/Инфушечка/Истинный фундамент/03 Строки, переменные и типы данных/03.15 Срезы строк|срезы строк]], [[Предметы/Инфушечка/Истинный фундамент/04 Функции/index|функции]], [[Предметы/Инфушечка/Истинный фундамент/05 Логика/index|условия и логика]], [[Предметы/Инфушечка/Истинный фундамент/06 Циклы/index|циклы]] и [[Предметы/Инфушечка/Истинный фундамент/07 Коллекции/07.1 Списки|списки]].

Данное задание удобнее всего решать кодом (хотя можно и ручками, но это дольше). Представляет из себя маленькую программу, 15-20 строк, которую надо написать по заданному алгоритму.

# Пример 1)

![[_Resours/Pasted image 20241030145642.png]]

Во-первых, определим [[Предметы/Инфушечка/Истинный фундамент/04 Функции/04.9 Создание функции|функцию]], которая будет переводить число в двоичную систему:

<div class="python-checker" data-title="Пример 1)" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    #print(bn)"></div>

Обратите внимание: мы срезаем 2 первых символа от преобразования в двоичную систему. Зачем? Вот за этим:

<div class="python-checker" data-title="Пример 1), шаг 2" data-code="n = 215&#10;bn = bin(n)&#10;print(bn)&#10;# Такой код выдаст 0b11010111"></div>

Почему жизнь так устроена? Че ему неймется простым 0 и 1? Откуда высер в виде некоего "0b" перед самим числом? Если кратко:

Префикс `0b` в Питон используется для обозначения чисел в двоичной системе, чтобы однозначно указать, что это двоичное представление, а не десятичное или другое. Этот префикс добавляется функцией [[Предметы/Инфушечка/Истинный фундамент/04 Функции/04.8 Стандартная библиотека|`bin()`]], и его наличие помогает различать числовые системы в коде, делая программу более понятной и предотвращая ошибки.

В других числовых системах Питон также использует аналогичные префиксы:

- **`0b`** для двоичных чисел (например, `0b11010111`),
- **`0o`** для восьмеричных чисел (например, `0o327`),
- **`0x`** для шестнадцатеричных чисел (например, `0xD7`).

Префиксы помогают интерпретатору Питон точно понимать систему счисления числа.

Спасибо, GPT, что бы я без тебя делал,,,

В общем да, чтобы получить само число, надо срезать 2 первых символа простым и классическим [[Предметы/Инфушечка/Истинный фундамент/03 Строки, переменные и типы данных/03.15 Срезы строк|срезом]].

Далее, рассуждаем так: т.к. это двоичное число, то сумма цифр может быть четной или нечетной только лишь за счет единиц. Т.е. само [[Предметы/Инфушечка/Истинный фундамент/05 Логика/05.8 Условие if|условие]] надо писать относительно единиц. Благо есть крутой метод, который сильно упростит жизнь - [[Предметы/Инфушечка/Истинный фундамент/04 Функции/04.4 Методы строк|count]]. Используется так:

<div class="python-checker" data-title="Пример 1), шаг 3" data-code="bn = &#x27;11010111&#x27;&#10;&#10;print(&#x27;Единиц:&#x27;, bn.count(&#x27;1&#x27;))&#10;print(&#x27;Четное количество единиц:&#x27;, bn.count(&#x27;1&#x27;) % 2 == 0)"></div>

Мы только что буквально сказали питону: "Братик, вот смотри, надо крч в переменной bn найти нечто, что называется "1" и если сумма этих "1" там делится на 2 без остатка, то,,,". Если подобное условие выполняется, тогда пусть будет некая другая переменная, к которой будем дописывать то, что просят по условию: нолик справа и 10 слева, если таки кратно 2 и 1 справа и 11 слева, если не кратно 2 а затем перевести обратно в десятичную систему:

<div class="python-checker" data-title="Пример 1), шаг 4" data-code="def f(n):&#10;    bn = bin(n)[2:] #Перевод в двоичну&#10;    #print(bn)&#10;    if bn.count(&#x27;1&#x27;) % 2 == 0: #Условие на четность&#10;        r = bn + &#x27;0&#x27; #Дописываем справа нолик&#10;        #print(r)&#10;        r = &#x27;10&#x27; + r[2:] #Дописывам слева 10&#10;        #print(r)&#10;    else: # Иначе&#10;        r = bn + &#x27;1&#x27; #Дописываем справа 1&#10;        #print(r)&#10;        r = &#x27;11&#x27; + r[2:] #Дописываем слева 11&#10;        #print(r)&#10;    #print(r)&#10;    r = int(r,2) #Переводим обратно в десятичную, здесь 2 - основание системы счисления, ИЗ которой переводим&#10;    #print(r)&#10;    return r # Возвращаем результат функции"></div>

Теперь у нас есть функция, которая делает все необходимые действия, красота. Осталось найти наименьшее n. Естественно, не вручную.

Давайте обозначим идею: пускай у нас будет [[Предметы/Инфушечка/Истинный фундамент/07 Коллекции/07.1 Списки|список]] (изначально пустой), в который мы будем докидывать n-ки, если наша f(n) окажется больше 40, что и нужно по условию. Попробуем:

<div class="python-checker" data-title="Пример 1), шаг 5" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    if bn.count(&#x27;1&#x27;) % 2 == 0:&#10;        r = bn + &#x27;0&#x27;&#10;        r = &#x27;10&#x27; + r[2:]&#10;    else:&#10;        r = bn + &#x27;1&#x27;&#10;        r = &#x27;11&#x27; + r[2:]&#10;    return int(r, 2)&#10;&#10;otv = []&#10;for n in range(1, 300):&#10;    if f(n) &gt; 40:&#10;        otv.append(n)&#10;&#10;print(min(otv))"></div>

Вуаля, у нас есть полный код:

<div class="python-checker" data-title="Пример 1), шаг 6" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    #print(bn)&#10;    if bn.count(&#x27;1&#x27;) % 2 == 0:&#10;        r = bn + &#x27;0&#x27;&#10;        #print(r)&#10;        r = &#x27;10&#x27; + r[2:]&#10;        #print(r)&#10;    else:&#10;        r = bn + &#x27;1&#x27;&#10;        #print(r)&#10;        r = &#x27;11&#x27; + r[2:]&#10;        #print(r)&#10;    #print(r)&#10;    r = int(r,2)&#10;    #print(r)&#10;    return r&#10;  &#10;otv = []&#10;for n in range (1,300):&#10;    if f(n)&gt;40:&#10;        otv.append(n)&#10;print(min(otv))"></div>

# Пример 2)

![[_Resours/Pasted image 20241030231021.png]]

Последняя часть программы с поиском максимального почти такая же, за исключением самого условия: теперь ищем не минимальное, а максимальное и не больше 40, а меньше 100:

<div class="python-checker" data-title="Пример 2)" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    if n % 3 == 0:&#10;        r = bn + bn[-3:]&#10;    else:&#10;        r = bn + bin((n % 3) * 3)[2:]&#10;    return int(r, 2)&#10;&#10;otv = []&#10;for n in range(1, 300):&#10;    if f(n) &lt; 100:&#10;        otv.append(n)&#10;&#10;print(max(otv))"></div>

А вот функция отличается в неприятную сторону. Для того, чтобы дописывать младшие разряды проще и понятнее всего использовать [[Предметы/Инфушечка/Истинный фундамент/03 Строки, переменные и типы данных/03.15 Срезы строк|отрицательные индексы среза]]:

<div class="python-checker" data-title="Пример 2), шаг 2" data-code="n = 6&#10;bn = bin(n)[2:]&#10;&#10;if n % 3 == 0:&#10;    r = bn + bn[-3:]&#10;&#10;print(r)"></div>

Если срезы забылись, вернитесь в [[Предметы/Инфушечка/Истинный фундамент/03 Строки, переменные и типы данных/03.15 Срезы строк|Истинный фундамент про срезы]].

<div class="python-checker" data-title="Пример 2), шаг 3" data-code="n = 5&#10;bn = bin(n)[2:]&#10;&#10;if n % 3 == 0:&#10;    r = bn + bn[-3:]&#10;else:&#10;    r = bn + bin((n % 3) * 3)[2:]&#10;&#10;print(r)"></div>

Тут у нас результат получается так: берется число bn, к нему прибавляется перевод двоичного числа - остатка от деления n на 3, умноженного на 3 (как обычно при использовании bin надо срезать 2 символа слева).

Наконец, переводим результат в десятичную int-ом и возвращаем r в функцию. По итогу имеем следующую функцию:

<div class="python-checker" data-title="Пример 2), шаг 4" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    #print(bn)&#10;    if n % 3 == 0:&#10;        r = bn + bn[-3:]&#10;    else:&#10;        r = bn + bin((n % 3)*3)[2:]&#10;    #print(r)&#10;    r = int(r,2)&#10;    #print(r)&#10;    return r"></div>

Ну и осталось соединить последнюю часть кода с этой функцией, получится полная программа:

<div class="python-checker" data-title="Пример 2), шаг 5" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    #print(bn)&#10;    if n % 3 == 0:&#10;        r = bn + bn[-3:]&#10;    else:&#10;        r = bn + bin((n % 3)*3)[2:]&#10;    #print(r)&#10;    r = int(r,2)&#10;    #print(r)&#10;    return r&#10;otv = []&#10;for n in range(1,300):&#10;    if f(n) &lt; 100:&#10;        otv.append(n)&#10;print(max(otv))"></div>

# Пример 3)

![[_Resours/Pasted image 20241031125234.png]]

Буквально то же самое, что и предыдущее задание, слово в слово, но теперь изменилось условие в цикле:

<div class="python-checker" data-title="Пример 3)" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    #print(bn)&#10;    if n % 3 == 0:&#10;        r = bn + bn[-3:]&#10;    else:&#10;        r = bn + bin((n % 3) * 3)[2:]&#10;    #print(r)&#10;    r = int(r,2)&#10;    #print(r)&#10;    return r&#10;&#10;otv = []&#10;for n in range (1,300):&#10;    if f(n) &gt;= 76:&#10;        otv.append(n)&#10;print(min(otv))"></div>

# Пример 4)

![[_Resours/Pasted image 20241031125429.png]]

Для разнообразия, покажу немного другой способ решения:

<div class="python-checker" data-title="Пример 4)" data-code="def f(N):&#10;    bN = bin(N)[2:]&#10;    #print(bN)&#10;    if N % 3 == 0:&#10;        R = bN + bN[-3:]&#10;    else:&#10;        R = bN + bin(((N % 3)*3))[2:]&#10;    #print(R)&#10;    R = int(R,2)&#10;    return R&#10;&#10;min = 10000&#10;for N in range (1,300):&#10;    if f(N) &gt; 151 and f(N) &lt; min:&#10;        min = f(N)&#10;print (min)"></div>

В данном случае min ищется более наглядно. В остальном, код все тот же.

# Пример 5)

![[_Resours/Pasted image 20241031125554.png]]

<div class="python-checker" data-title="Пример 5)" data-code="def f(n):&#10;    bn = bin(n)[2:]&#10;    #print(bn)&#10;    if n % 2 == 0:&#10;        R = &#x27;10&#x27;+ bn&#10;    else:&#10;        R = &#x27;1&#x27; + bn + &#x27;01&#x27;&#10;    #print(R)&#10;    R = int(R,2)&#10;    #print(R)&#10;    return R&#10;otv = []&#10;for n in range (1,1000):&#10;    if f(n) &gt; 516:&#10;        otv.append(n)&#10;print(min(otv))"></div>

В этой задаче у нас немного поменялись условия внутри функции.

# Пример 6)

![[_Resours/Pasted image 20241031125829.png]]

<div class="python-checker" data-title="Пример 6)" data-code="def f(N):&#10;	bN = bin(N)[2:]&#10;	if N % 2 == 0:&#10;		R = &#x27;10&#x27;+ bN&#10;	else:&#10;		R = &#x27;1&#x27; + bN + &#x27;01&#x27;&#10;	R = int(R,2)&#10;	return (R)&#10;&#10;for N in range (1,13):&#10;    print(f(N))"></div>

А вот тут можно запустить код и посмотреть все значения: самих итераций цикла не очень много.
На примере этого же задания как можно еще короче решать, без определения функции:

<div class="python-checker" data-title="Пример 6), шаг 2" data-code="maxi = 0&#10;for n in range(1,12 + 1):&#10;    s = bin(n)[2:]&#10;    if n % 2 == 0:&#10;        s = &quot;10&quot; + s&#10;    else:&#10;        s = &quot;1&quot; + s + &quot;01&quot;&#10;    r = int(s, 2)&#10;    maxi = max(maxi, r)&#10;print(maxi)"></div>

Само [[Предметы/Инфушечка/Истинный фундамент/05 Логика/05.8 Условие if|условие `if`]] осталось нетронутым, преобразование `int` и `bin` в том же месте, написаны также. Разница в том, что мы сразу же указываем [[Предметы/Инфушечка/Истинный фундамент/06 Циклы/index|цикл]] и пишем в нем же условие. Менее наглядно, зато быстрее.

---
# Пример 7)

![[Pasted image 20260826150233.png]]

<div class="python-checker" data-title="Пример 7)" data-code="# №25344&#10;&#10;def tri(n):&#10;    s = &#x27;&#x27;&#10;    while n &gt; 0:&#10;        s = str(n % 3) + s&#10;        n //= 3&#10;    return s&#10;&#10;&#10;def f(n):&#10;    s = tri(n)&#10;&#10;    if n % 3 == 0:&#10;        r = s + s[-2:]&#10;    else:&#10;        r = s + tri(sum(map(int, s)) * 3)&#10;&#10;    return int(r, 3)&#10;&#10;&#10;otv = []&#10;for n in range(1, 1000):&#10;    if f(n) &gt; 208 and f(n) % 2 == 1:&#10;        otv.append(f(n))&#10;&#10;print(min(otv))"></div>

# Пример 8)

![[Pasted image 20260826150256.png]]

<div class="python-checker" data-title="Пример 8)" data-code="# №27617&#10;&#10;def f(n):&#10;    s = bin(n)[2:]&#10;&#10;    if n % 3 == 0:&#10;        r = s + s[-3:]&#10;    else:&#10;        r = s + bin((n % 3) * 3)[2:]&#10;&#10;    return int(r, 2)&#10;&#10;&#10;mn = 10**10&#10;otv = []&#10;&#10;for n in range(1, 1000):&#10;    if abs(f(n) - 130) &lt; mn:&#10;        mn = abs(f(n) - 130)&#10;        otv = [n]&#10;    elif abs(f(n) - 130) == mn:&#10;        otv.append(n)&#10;&#10;print(max(otv))"></div>

Вот тут остановимся чуть подробнее. Нас очень интересует условие:

> ... число R, ближайшее к 130.

Что значит ближайшее? Это значит такое, у которого расстояние на координатной прямой до `R` наименьшее. Т.е. `|R - 130|` должно быть наименьшим. А как найти наименьшее? Тут мы не паримся, ищем минимум "школьным" способом. Буквально, если у нас эта разность `abs(f(n) - 130) < mn` т.е. якобы минимального, с которым мы сравниваем, то `mn` становится новым минимумом, а само `n` мы запоминаем. Ежели равенство т.е. `elif abs(f(n) - 130) == mn` тогда просто добавляем в массив, потом разберемся `max(otv)`.

---
# Пример 9)

![[Pasted image 20260826151121.png]]

<div class="python-checker" data-title="Пример 9)" data-code="# №29337&#10;&#10;def f(n):&#10;    s = bin(n)[2:]&#10;&#10;    if s.count(&#x27;1&#x27;) % 2 == 0:&#10;        r = s + &#x27;0&#x27;&#10;        r = &#x27;10&#x27; + r[2:]&#10;    else:&#10;        r = s + &#x27;1&#x27;&#10;        r = &#x27;11&#x27; + r[2:]&#10;&#10;    return int(r, 2)&#10;&#10;&#10;otv = []&#10;for n in range(1, 1000):&#10;    if f(n) &lt;= 19:&#10;        otv.append(n)&#10;&#10;print(max(otv))"></div>

# Пример 10)

![[Pasted image 20260826151152.png]]

<div class="python-checker" data-title="Пример 10)" data-code="# №29956&#10;&#10;def tri(n):&#10;    s = &#x27;&#x27;&#10;    while n &gt; 0:&#10;        s = str(n % 3) + s&#10;        n //= 3&#10;    return s&#10;&#10;&#10;def f(n):&#10;    s = tri(n)&#10;&#10;    if n % 3 == 0:&#10;        r = &#x27;1&#x27; + s + &#x27;02&#x27;&#10;    else:&#10;        r = s + tri((n % 3) * 5)&#10;&#10;    return int(r, 3)&#10;&#10;&#10;otv = []&#10;for n in range(1, 1000):&#10;    if f(n) &gt;= 177:&#10;        otv.append(n)&#10;&#10;print(min(otv))"></div>

# Пример 11)

![[Pasted image 20260826151516.png]]

<div class="python-checker" data-title="Пример 11)" data-code="# Чуть другой метод решения, через 2 функции&#10;&#x27;&#x27;&#x27;def d(n):&#10;    s = &#x27;&#x27;&#10;    while n &gt; 0:&#10;        s = str(n % 3) + s&#10;        n = n // 3&#10;    return s&#10;&#10;def f(n):&#10;    k = d(n)&#10;    if n % 3 == 0:&#10;        k += k[-3:]&#10;    else:&#10;        k = k + d((n % 3) * 3)&#10;    k = int(k,3)&#10;    return k&#10;&#10;otv = []&#10;for n in range (1,100):&#10;    if f(n) &gt; 150:&#10;        otv.append(n)&#10;print(min(otv))&#10;&#x27;&#x27;&#x27;&#10;&#10;# Можно отдельно скринить / сохранять себе куда-нибудь&#10;# Алгоритм перевода в любую произвольную систему счисления&#10;def d(n):&#10;    s = &#x27;&#x27;&#10;    while n &gt; 0:&#10;        s = str(n % 3) + s&#10;        n = n // 3&#10;    return s&#10;&#10;otv = []&#10;for n in range(1,100):&#10;    k = d(n)&#10;    if n % 3 == 0:&#10;        k += k[-3:]&#10;    else:&#10;        k = k + d((n % 3) * 3)&#10;    r = int(k,3)&#10;    if r &gt; 150:&#10;        otv.append(n)&#10;print(min(otv))"></div>

---
# Еще один алгоритм перевода в произвольную систему

В данном случае - в троичную:

<div class="python-checker" data-title="Еще один алгоритм перевода в произвольную систему" data-code="def troika(x):&#10;   s = &#x27;&#x27;&#10;   while x &gt; 0:&#10;       s += str(x % 3)&#10;       x //= 3&#10;   s = s[::-1]&#10;   return s"></div>
