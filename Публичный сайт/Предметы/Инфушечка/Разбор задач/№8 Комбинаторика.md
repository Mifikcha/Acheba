
Это задание довольно легко прогается с некоторыми нюансами по отдельным номерам. В 2026 году изменений в задаче не было, осталась как есть.

# Пример 1)

![[_Resours/Pasted image 20241105233503.png]]

На самом деле, 8ое задание прогой решается полным перебором в любом из возможных вариантов. Возьмем для примера эту задачу.

Идея в том, чтобы тупо написать все возможные слова и вручную найти нужное т.е. то, которое начинается с буквы у. Для этого придется задать алфавит, счетчик и число букв в словах.

> [!tip] Из хорошего
> В целом, этот алгоритм практически универсален, с поправкой на сложные номера. Идея та же, условий будет больше.

<div class="python-checker" data-title="Пример 1" data-code="alp = &quot;АКРУ&quot;&#10;c = 1 # Счетчик номера чисел&#10;for l1 in alp: # Циклы букв. Их тут именно 5 потому, что, по условию слова 5-буквенные. Сколько букв - столько и циклов&#10;    for l2 in alp:&#10;        for l3 in alp:&#10;            for l4 in alp:&#10;                for l5 in alp:&#10;                    word = l1 + l2 + l3 + l4 + l5 # Задаем само слово&#10;                    print (c, word) # Принтуем сначала номер, потом слово&#10;                    c += 1 # Накидываем счетчик"></div>

> [!warning] ВАЖНО
> Буквы пишем в том порядке, в котором они даны НЕ в тексте задания, а в самом списке, здесь они совпадают, но иногда - нет

Программка выдаст список из 1024 слов. 769 из них - это первое слово, которое начинается с буквы У.

**_Ответ:_** 769

# Пример 2)

![[_Resours/Pasted image 20241105235027.png]]

Собственно, велосипед не изобретаем, алфавит меняем, циклы расширяем. Единственное, что кол-во слов больше, около 15к, заебемся искать. Поэтому напишем дополнительное условие прям в цикле:

<div class="python-checker" data-title="Пример 2" data-code="alp = &quot;КЛНТЭ&quot;&#10;c = 1&#10;for l1 in alp:&#10;    for l2 in alp:&#10;        for l3 in alp:&#10;            for l4 in alp:&#10;                for l5 in alp:&#10;                    for l6 in alp:&#10;                        word = l1 + l2 + l3 + l4 + l5 + l6&#10;                        if word == &quot;ККЛКЛК&quot;: print (c, word)&#10;                        c += 1"></div>

> [!note] По поводу циклов
В №8 вместо того, чтобы писать какие буквы должны быть в слове, а какие нет под `if`, можно исключить некоторые буквы из `alp`. Эти действия похожи, но не совсем эквивалентны. Когда вас в задании просят номер слова, то лучше условия писать под `if`, а не исключать буквы из `alp`.

**_Ответ:_** 131.

# Пример 3)

![[_Resours/Pasted image 20240926185915.png]]

По той же схеме:

<div class="python-checker" data-title="Пример 3" data-code="alp = &quot;ABCEDF&quot;&#10;&#10;c = 0&#10;&#10;for a1 in alp:&#10;    for a2 in alp:&#10;        for a3 in alp:&#10;            for a4 in alp:&#10;                for a5 in alp:&#10;                    w = a1 + a2 + a3 + a4 + a5&#10;                    if w[0] != &quot;F&quot; and w[4] != &quot;A&quot;:&#10;                        c+=1&#10;print(c)"></div>

# Пример 4)

![[_Resours/Pasted image 20260827152745.png]]

Как и говорил, идея та же, условий чуть побольше. Следующие несколько номеров, думаю, можно с минимальными комментариями.

<div class="python-checker" data-title="Пример 4" data-code="# №25347&#10;&#10;alp = &quot;АГИНРТ&quot;&#10;c = 1&#10;&#10;for l1 in alp:&#10;    for l2 in alp:&#10;        for l3 in alp:&#10;            for l4 in alp:&#10;                for l5 in alp:&#10;                    for l6 in alp:&#10;                        word = l1 + l2 + l3 + l4 + l5 + l6&#10;&#10;                        if c % 2 == 1:&#10;                            if word[0] not in &quot;АИГ&quot;:&#10;                                if word.count(&quot;А&quot;) == 1:&#10;                                    print(c, word)&#10;                                    exit()&#10;&#10;                        c += 1"></div>

# Пример 5)

![[_Resours/Pasted image 20260827153000.png]]

Тут нас просят найти номер последнего слова с таким-то условиями. Можно просто поставить еще одну переменную и, поскольку циклы идут от меньшего к большему, то последнее слово само запишется в эту переменную именно в нужный момент.

<div class="python-checker" data-title="Пример 5" data-code="# №27620&#10;&#10;alp = &quot;ИРСТУЦ&quot;&#10;c = 1&#10;ans = 0&#10;&#10;for l1 in alp:&#10;    for l2 in alp:&#10;        for l3 in alp:&#10;            for l4 in alp:&#10;                for l5 in alp:&#10;                    word = l1 + l2 + l3 + l4 + l5&#10;&#10;                    if word.count(&quot;И&quot;) == 2:&#10;                        if &quot;ЦЦ&quot; not in word:&#10;                            ans = c&#10;&#10;                    c += 1&#10;&#10;print(ans)"></div>

# Пример 6)

![[_Resours/Pasted image 20260827153543.png]]

<div class="python-checker" data-title="Пример 6" data-code="# №31216&#10;&#10;alp = &quot;ВЕКОТЦ&quot;&#10;c = 1&#10;ans = 0&#10;&#10;for l1 in alp:&#10;    for l2 in alp:&#10;        for l3 in alp:&#10;            for l4 in alp:&#10;                for l5 in alp:&#10;                    for l6 in alp:&#10;                        word = l1 + l2 + l3 + l4 + l5 + l6&#10;&#10;                        if c % 2 == 1:&#10;                            if &quot;В&quot; not in word and &quot;Е&quot; not in word and &quot;К&quot; not in word:&#10;                                if word.count(&quot;Т&quot;) == 2:&#10;                                    if word.count(&quot;Ц&quot;) == 1:&#10;                                        ans += 1&#10;&#10;                        c += 1&#10;&#10;print(ans)"></div>

# Пример 7)

![[_Resours/Pasted image 20240926190144.png]]

Аналогично предыдущим, меняются только условия. В случае этого задания нужно 3 условия:

1) Чтобы первое число не было равно 0, иначе число не будет восприниматься как пятизначное и мы получим неправильный ответ.
2) Чтобы в числе было ровно 2 тройки.
3) Чтобы никоим образом мы не встретили внутри числа одну из следующих комбинаций: `[12 21 23 32 52 25 72 27]`.

<div class="python-checker" data-title="Пример 7" data-code="alp = &#x27;012345678&#x27;&#10;count = 0&#10;for l1 in alp:&#10;    for l2 in alp:&#10;        for l3 in alp:&#10;            for l4 in alp:&#10;                for l5 in alp:&#10;                    number = l1 + l2 + l3 + l4 + l5&#10;                    if number[0] != &#x27;0&#x27;:&#10;                        if number.count(&#x27;3&#x27;) == 2:&#10;                            if all(x not in number for x in &#x27;12 21 23 32 52 25 72 27&#x27;.split()):&#10;                                count += 1&#10;print(count)"></div>

Разберемся с третьим условием подробнее.

```python
if all(x not in number for x in '12 21 23 32 52 25 72 27'.split()):
```

Этот кусок кода проверяет, что в числе `number` нет запрещенных комбинаций `"12"`, `"21"`, `"23"`, `"32"`, `"52"`, `"25"`, `"72"`, `"27"`.

Это работает так: `'12 21 23 32 52 25 72 27'.split()` превращает строку в список: `['12', '21', '23', '32', '52', '25', '72', '27']`. Затем, в `for x in ['12', '21', '23', '32', '52', '25', '72', '27']` мы перебираем каждую запрещенную последовательность (`x`). `x not in number`: тут проверяем, содержится ли `x` в `number`. Если `number = "12345"`, `"12"` есть в `number`, значит `x not in number` → ложь.  Если `number = "34567"`, `"12"` нет, значит `x not in number` → истина.

`all([...])` проверяет, **что все элементы** списка дают `True`, т.е. ни одной запрещенной комбинации **нет** в числе.

### Пример работы

#### Пример 1) (Запрещенная комбинация есть)

```python
number = "12345"
```
- `'12' in "12345"` → `True`
- `'21' in "12345"` → `False`
- `'23' in "12345"` → `True`
- Остальные комбинации не проверяются, так как `all([...])` уже **не выполняется**.
- **Условие не проходит, число не учитывается**.

#### Пример 2) (Все запрещенные комбинации отсутствуют)

```python
number = "30456"
```
- `'12' not in "30456"` → `True`
- `'21' not in "30456"` → `True`
- `'23' not in "30456"` → `True`
- `'32' not in "30456"` → `True`
- `'52' not in "30456"` → `True`
- `'25' not in "30456"` → `True`
- `'72' not in "30456"` → `True`
- `'27' not in "30456"` → `True`
- **Все элементы вернули `True`, значит `all([...]) == True`**, и число засчитывается.

Таким образом, каждый из нас - булочка с корицей,,,

**_Ответ:_** 3352.

# Пример 8)

![[_Resours/Pasted image 20251201193630.png]]

Тут мы можем выделить один хороший прием, ответ на условие про "не содержит идущих подряд одинаковых цифр". Прием называется "пробегись по всей длине строки и проверь, что текущее не равно следующему". Реализуется так:

<div class="python-checker" data-title="Пример 8" data-code="digits = &quot;0123456&quot;&#10;count = 0&#10;&#10;for d1 in digits[1:]: # С нуля не начинаем&#10;    for d2 in digits:&#10;        for d3 in digits:&#10;            for d4 in digits:&#10;                for d5 in digits:&#10;                    num = d1 + d2 + d3 + d4 + d5&#10;&#10;                    if num.count(&#x27;6&#x27;) == 1 and all(num[i] != num[i+1] for i in range(4)):&#10;                        count += 1&#10;&#10;print(count)"></div>

Вот как раз `all(num[i] != num[i+1] for i in range(4))` - буквально означает "выполняется для всех случаев (`all`), что текущая цифра (`num[i]`) не равна (`!=`) следующей цифре (`num[i+1]`) и так для каждой позиции из всего числа (`for i in range(4)`)". Работаю переводчиком, номер знаете.

# Пример 9)

Также, в №8 есть прототипы посложнее. Для них наш шаблон тоже подходит. Рассмотрим одно из таких:

![[_Resours/Pasted image 20241106002223.png]]

Идея кода такая же, как и в предыдущих случаях - полный перебор. Меняются только условия. Тут нужно рассмотреть несколько важных случаев и составить дерево условий:

1) Если 5 находится вначале числа (на первой позиции), то нужно проверить является ли число впереди него одним из нечетных т.е. `1, 3, 7`. Если да - не засчитываем в ответ.
2) Если 5 находится на последней позиции числа, то нужно проверить является ли число на предпоследней позиции одним из нечетных т.е. `1, 3, 7`. Если да - также не засчитываем в ответ.
3) Если 5 находится где-то в середине числа, то нужно проверить соседние числа, с индексом (позицией) на 1 больше и на 1 меньше, чем у 5. Если кто-то из них нечетный, то не засчитываем эту итерацию цикла в ответ.
4) В остальных случаях - нас все устраивает, прибавляем счетчик `count += 1`.

По итогу, получаем такой код:

<div class="python-checker" data-title="Пример 9" data-code="alp = &quot;012345678&quot;  # Девятеричная система&#10;count = 0&#10;&#10;for l1 in alp[1:]:  # Первая цифра не может быть 0&#10;    for l2 in alp:&#10;        for l3 in alp:&#10;            for l4 in alp:&#10;                for l5 in alp:&#10;                    number = l1 + l2 + l3 + l4 + l5&#10;                    &#10;                    # Проверяем, что ровно одна цифра 5&#10;                    if number.count(&quot;5&quot;) == 1:&#10;                        continue&#10;                    &#10;                    # Определяем позицию 5&#10;                    index_5 = number.index(&quot;5&quot;)&#10;&#10;                    # Проверяем условия для соседей&#10;                    if index_5 == 0:  # 5 в начале числа&#10;                        if number[1] in &quot;137&quot;: &#10;                            continue&#10;                    elif index_5 == 4:  # 5 в конце числа&#10;                        if number[3] in &quot;137&quot;: &#10;                            continue&#10;                    else:  # 5 внутри числа&#10;                        if number[index_5 - 1] in &quot;137&quot; or number[index_5 + 1] in &quot;137&quot;:&#10;                            continue&#10;&#10;                    count += 1&#10;&#10;print(count)"></div>

**_Ответ:_** 8880


# Пример 10)

![[_Resours/Pasted image 20240923195646.png]]

Довольно хитрый прием в восьмом номере - это делать так, чтобы цифры были различны и / или никакие две четные / нечетные не стояли рядом. В целом, решать можно по-разному, я покажу два способа.

### Способ 1) Два цикла и тупые условия

Идея такая: вместо того, чтобы писать один большой цикл, напишем 2 разных цикла, а результат сложим. Такая практика нужна, прежде всего для удобного выполнения обоих условий одновременно: "две четные и две нечетные не стоят рядом" у нас правда потому, что мы буквально чередуем чет / нечет в циклах, а различность цифр выполняется потому, что мы буквально чередуем чет / нечет в циклах.

> [!note] Совет
> Не рекомендую конкретно в этом задании в алфавите циклов самостоятельно исключать числа - программа все решит за вас.

Однако, дополнительные условие под `if` написать все же придется, поскольку `l1` у нас может совпадать с `l3` и `l5`, также как `l2` может совпадать с `l4`. В целом, дело нехитрое.

<div class="python-checker" data-title="Способ 1 Два цикла и тупые условия" data-code="alp = &quot;01234567&quot;&#10;c = 0&#10;for l1 in &quot;246&quot;: # Числа не начинаются с 0&#10;    for l2 in &quot;357&quot;: &#10;        for l3 in &quot;0246&quot;: &#10;            for l4 in &quot;357&quot;: &#10;                for l5 in &quot;0246&quot;:&#10;                    word = l1 + l2 + l3 + l4 + l5&#10;                    if l1 != l3 and l1 != l5 and l5 != l3 and l2 != l4:&#10;                        c += 1&#10;for l1 in &quot;357&quot;:&#10;    for l2 in &quot;0246&quot;: &#10;        for l3 in &quot;357&quot;: &#10;            for l4 in &quot;0246&quot;: &#10;                for l5 in &quot;357&quot;:&#10;                    word = l1 + l2 + l3 + l4 + l5&#10;                    if l1 != l3 and l1 != l5 and l5 != l3 and l2 != l4:&#10;                        c += 1&#10;print(c)"></div>

### Способ 2) Через `itertools`

Собственно, тут тоже полный перебор, но написано хитрее. Давайте сначала код посмотрим, а потом расскажу детали.

<div class="python-checker" data-title="Способ 2 Через itertools" data-code="from itertools import product&#10;&#10;c = 0&#10;&#10;for w in product(&#x27;0234567&#x27;, repeat=5):&#10;    if w[0] != &#x27;0&#x27; and len(set(w)) == 5:&#10;        if all(int(w[i]) % 2 != int(w[i + 1]) % 2 for i in range(4)):&#10;            c += 1&#10;&#10;print(c)"></div>

По шагам:

* `for w in product('0234567', repeat=5):` - перебирает все комбинации из цифр `0234567` длины 5
* `if w[0] != '0' and len(set(w)) == 5:` - выполняет условия, что число не начинается с `0` и что все числа различны
* `if all(int(w[i]) % 2 != int(w[i + 1]) % 2 for i in range(4)):` и далее `c+=1` - цитата: "если для всех соседних пар цифр остатки от деления на 2 разные, значит рядом нигде не стоят две чётные или две нечётные цифры и можно делать `c+=1`"


# Пример 11)

![[_Resours/Pasted image 20240926190418.png]]

Логика точно та же, что и в предыдущем примере, только вместо чисел у нас строки. Не страшно.

### Два цикла

<div class="python-checker" data-title="Два цикла" data-code="c = 0&#10;&#10;gl = &#x27;ОИА&#x27;&#10;sg = &#x27;ПЛН&#x27;&#10;&#10;for l1 in gl:&#10;    for l2 in sg:&#10;        for l3 in gl:&#10;            for l4 in sg:&#10;                for l5 in gl:&#10;                    for l6 in sg:&#10;                        word = l1 + l2 + l3 + l4 + l5 + l6&#10;                        if len(set(word)) == 6:&#10;                            c += 1&#10;&#10;for l1 in sg:&#10;    for l2 in gl:&#10;        for l3 in sg:&#10;            for l4 in gl:&#10;                for l5 in sg:&#10;                    for l6 in gl:&#10;                        word = l1 + l2 + l3 + l4 + l5 + l6&#10;                        if len(set(word)) == 6:&#10;                            c += 1&#10;&#10;print(c)"></div>

Тут, однако, нам придется руками сказать компьютеру мол вон смотри вот это гласные, а вот это согласные. Условие `if len(set(word)) == 6:` проверяет различность, как и прежде.

### Через `itertools`

<div class="python-checker" data-title="Через itertools" data-code="from itertools import product&#10;&#10;c = 0&#10;&#10;letters = &#x27;ПОЛИНА&#x27;&#10;gl = &#x27;ОИА&#x27;&#10;sg = &#x27;ПЛН&#x27;&#10;&#10;for w in product(letters, repeat=6):&#10;    if len(set(w)) == 6:&#10;        if all((w[i] in gl) != (w[i + 1] in gl) for i in range(5)):&#10;            c += 1&#10;&#10;print(c)"></div>

Или даже можно вот так:

<div class="python-checker" data-title="Через itertools" data-code="from itertools import permutations&#10;&#10;c = 0&#10;&#10;gl = &#x27;ОИА&#x27;&#10;&#10;for w in permutations(&#x27;ПОЛИНА&#x27;):&#10;    if all((w[i] in gl) != (w[i + 1] in gl) for i in range(5)):&#10;        c += 1&#10;&#10;print(c)"></div>

Тут можно и без комментариев, полная аналогия с предыдущим.
