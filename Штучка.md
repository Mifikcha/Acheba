### Math engine probe

(1) Inline basic:
$c^2 = a^2 + b^2$  ← Должно работать везде

(2) Block basic:
$$ E = mc^2 $$  ← Должно работать везде

(3) AMS align:
$$
\begin{align}
a+b &= c\\
x-y &= z
\end{align}
$$
Ожидается: работает и в MathJax, и в KaTeX (новых)

(4) Numbering via \tag:
$$
F = ma \tag{★}
$$
Ожидается: работает в обоих

(5) Negative thin space \!:
$\sin\!x + \cos\!x$
Если виден буквальный “!”, значит движок/настройка не распознаёт \!

(6) \bbox (MathJax специфично):
$${x^2+y^2}$$
MathJax: работает; KaTeX: обычно НЕ работает

(7) \htmlClass (MathJax специфично):
$$\htmlClass{myclass}{\int_0^1 f(x)\,dx}$$
MathJax: работает; KaTeX: НЕ работает

(8) \require (MathJax специфично):
$$ \require{physics} \dv{y}{x} $$
MathJax: работает (если разрешены расширения); KaTeX: НЕ работает

(9) mhchem:
$$ \ce{H2O + CO2 -> H2CO3} $$
MathJax с mhchem: работает; KaTeX: работает только если включена поддержка mhchem плагином

(10) \eqref + \label (часто без автонумерации):
См. уравнение \eqref{eq:test}.
$$
\begin{aligned}
e^{i\pi}+1=0 \label{eq:test}
\end{aligned}
$$
Обычно рендерится без референс-линков в Obsidian по умолчанию. Если прямые ссылки срабатывают — это явно MathJax с включенной нумерацией/плагином.

(11) \color с HEX:
$\color{#0099cc}{x^2}$
Обычно ок у обоих, но в KaTeX раньше бывали нюансы.

(12) \newcommand:
$\newcommand{\RR}{\mathbb{R}} \RR^n$
И MathJax, и KaTeX обычно поддерживают макросы, но у KaTeX ограничения, если макросы в инлайне — смотри, применился ли \RR.
