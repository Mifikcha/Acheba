Что я понял:

В статье рассматривается метод, позволяющий убрать лишние, непрезентативные признаки из датасетов. Делается это с помощью матрицы ковариации, вычисляя которую можно определить насколько далеко в смысле специального расстояния, представленного в статье, между подпространствами находятся те или иные признаки. Таким образом, можно избавиться от излишнего усложнения многих алгоритмов кластеризации и упростить процесс обучения моделей.

Также, в статье представлен алгоритм, позволяющий посчитать такую матрицу.

Далее, методика была апробирована на датасетах различной степени изменчивости, тематики и в целом сложности. Для оценки точности кластеризации использовались ACC и NMI.

Касаемо графиков: графики, которые ты мне показывал - это графики зависимости точности ACC и NMI от кол-ва отобранных признаков различными алгоритмами кластеризации на представленных датасетах. И типа видно, что их самый крутой.

Там где бары (рисунок 4 и 5) - это просто сравнение точностей на ACC и NMI.

Хитмап (рисунок 6) - это показания ACC алгоритма VCSDFS (основного, который они рассматривают) на всех датасетах в зависимости от кол-ва выбранных признаков и параметра ρ (где параметр ρ - это просто коэффициент в их матричных уравнениях).