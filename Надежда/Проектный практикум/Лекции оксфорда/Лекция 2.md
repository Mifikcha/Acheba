Линейное обучение с учителем: ПРЯМАЯ связь между входными данными и выходными данными.

Довольно много процессов могут быть усреднены линейной моделью
Линейные модели можно применять для обучения больших моделей
Линейные проблемы могут быть решены аналитически
Линейные предсказания служат основой для вход в МЛ

Далее идет очень много материала из Воронцова, первые несколько тем

Введение (посмотреть есть ли представление предсказания и функции потерь в матричном виде, если нет - добавить, разузнать про то как найти Θ), линейная регрессия, функция потерь

В качестве параметров ищутся обычно точки пересечения с осью Y и наклон прямой





Из отличий: в качестве $x_{1:n}$ обозначены не просто признаки объекта, а, как выражается лектор, оси (axes) или векторы из $\mathbb{R}^d$, где d - число компонент вектора. 

Для линейной регрессии считаем, что есть только 1 число на выходе - $y_{i}$.

Хотим мы приблизительно следующее: чтобы моделька поучилась по заданному изначально датасету, а потом, когда мы даем новые признаки она нам дала предсказанный ответ для него.

![[Обучатор предсказатор.png]]

Одна из главных проблем таких моделей - если какая-то точка сильно выпадает по значениям из датасета, то она дает слишком сильный перекос
