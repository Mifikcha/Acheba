## Методы форматирования графиков

Основные методы добавления элементов:

- plt.xlabel('text') - подпись оси x
- plt.ylabel('text') - подпись оси y
- plt.title('text') - заголовок графика
- plt.grid() - сетка
- plt.legend(loc, bbox_to_anchor, title, frameon, fontsize) - легенда:
    
- loc: Указывает положение легенды на графике. Возможные значения: 'best': Выбирает оптимальное расположение автоматически; 'upper right', 'upper left', 'lower left', 'lower right', 'center left', 'center right', 'center', 'upper center', 'lower center', 'outside'
- bbox_to_anchor: Позволяет задать точное положение легенды в виде координат от 0 до 1.
- title: Заголовок легенды.
- frameon: Включает или отключает рамку вокруг легенды (True или False).
- fontsize: Размер шрифта в легенде.

Основные методы форматирования графика:

- plt.xlim(минимум, максимум) - ограничение значения оси x
- plt.ylim(минимум, максимум) - ограничение значения оси y
- plt.text(координата x, координата y, 'текст') - добавляет текст для заданной точки
- plt.xticks(), plt.xticks() - форматирование меток оси X, Y:
- ticks: Список значений, соответствующих положениям меток на оси X.
- labels: Список строковых меток, которые будут отображаться для каждого значения в ticks. Если не указан, будут использоваться значения из ticks.
- kwargs: Дополнительные ключевые аргументы для форматирования меток, такие как rotation, fontsize, fontweight и другие.

#### Пример

```python

# Пример построения графика с форматирование меток оси X
import matplotlib.pyplot as plt
import numpy as np

# Создаем данные для графика
x = np.linspace(0, 10, 100)
y = np.sin(x)

# Строим график, label - текст для легенды
plt.plot(x, y, label = 'sin(x)')

# Настраиваем метки на оси X
plt.xticks([0, 3.14, 6.28], ['0', 'π', '2π'], fontsize=14, color='red', rotation = 45)
plt.xlabel('Угол (рад)', fontsize=16)

# Добавляем сетку
plt.grid(True)
# заголовок
plt.title('Синус', fontsize=18)
# Добавляем легенду в нижний левый угол
plt.legend(loc='lower left')
# Отображаем график
plt.show()
```

#### Пример

```python

# Построим график сигмоиды.
import matplotlib.pyplot as plt
import numpy as np

t = np.linspace(-10, 10, 100)
sig = 1 / (1 + np.exp(-t))

plt.plot(t, sig, linewidth=2, label=r"$\sigma(t) = \frac{1}{1 + e^{-t}}$")
plt.legend()
plt.show()
```

#### Пример

```python

# Пример построения графика с форматирование легенды
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 5, 100)
y1 = np.sin(x)
y2 = np.cos(x)

plt.plot(x, y1)
plt.plot(x, y2)

#Добавляем легенду
plt.legend(loc='center left', ncol=2, labels=['sin(x)', 'cos(x)'])
plt.show()
```

### Метод show

Jupyter Notebook позволяет выводить график внутри своего окружения (в рабочей области). В стандартном файле python (*.py) отобразить рисунок позволяет метод show:

```python

plt.show()
```

#### Пример

```python

# Создайте файл plt_show.py, скопируйте туда текст программы и запустите его в Visual Studio Code или PyCharm

import numpy as np
import matplotlib.pyplot as plt
import math

x = np.arange(-2*np.pi, 2*np.pi, 0.1)
y1= [math.sin(i) for i in x]
y2= [math.cos(i) for i in x]
y3= [math.tanh(i) for i in x]

fig = plt.figure(figsize=(12, 4), layout='constrained')

plt.plot(x, y1,'b--', label='sin(x)')
plt.plot(x, y2,'r--', label='cos(x)')
plt.plot(x, y3,'mo-', label='tahn(x)')
plt.title('График тригонометрических функций')
plt.legend()
# Выведем окно с графиками
plt.show()
```

### Метод save

Сохранить полученный рисунок с графиками можно с помощью метода savefig:

```python

plt.savefig(filepath, [dpi])
```

Параметры:

- filepath - путь для сохранения графика.
- dpi - разрешение в точках на дюйм. Если поставить значение «figure», использует значение dpi полотна.

#### Пример

```python

# Пример сохранения графика 
import math
import matplotlib.pyplot as plt
import numpy as np

# Пустое полотно можно сохранять в переменную и редактировать его в любом месте программы
fig = plt.figure(num=1, figsize=(9,3))
N = 10
# используем logspace чтобы задать логарифмическое распределение от 0.1 до 1 для 10 точек. 
# endpoint - включает последний элемент в набор данных
x1 = np.logspace(start = 0.1, stop = 1, num = 10, endpoint=True)
x2 = np.logspace(start = 0.1, stop = 1, num = 10, endpoint=False)
y = np.zeros(N)

plt.plot(x1, y, 'o')
plt.plot(x2, y+0.5, 'o-')

# Зададим ограничение для оси Y
plt.ylim([-0.5, 1])

fig.legend(["x1", "x2"])
# Сохраним рисунок в файл logspace.jpg, с разрешением - 50
plt.savefig('logspace.jpg', dpi=50)
```

## Формат ввода

Постройте график метрик точности обученной модели

```python

import matplotlib.pyplot as plt
import pandas as pd

#в качестве данных возьмем результаты обучения модели детекции графических объектов
df = pd.read_csv('results_model_A.csv')
# в качестве x - выберем из датасета итерации обучения модели - эпохи
x = df['epoch']
# первый график будет отражать точность модели детекции
y=df['metrics/precision(B)']

plt.figure(figsize=(5, 2.7), layout='compressed')

#добавим значение y на график, подпись, и зададим стиль
# ваш код тут

y=df['metrics/recall(B)']
# добавим график с метрикой 'Полнота', цвет линии зададим с помощью параметра color

# ваш код тут

# выведем добавленные label в легенду

# ваш код тут

# Добавим заголовок, отформатируем его для примера: цвет зеленый, размер шрифта = 25

# ваш код тут
```