
|   |   |
|---|---|
|Ограничение времени|1000 секунд|
|Ограничение памяти|922Mb|
|Ввод|стандартный ввод или input.txt|
|Вывод|стандартный вывод или output.txt|

## Формат ввода

- Сгенерируйте синтетические данные

```
# X в диапозоне от 0 до 5, всего 40 образцов
X = np.sort(5 * np.random.rand(40, 1), axis=0)
y = np.sin(X).ravel()

# Добавляем шума для целевой переменной
y[::5] += 3 * (0.5 - np.random.rand(8))
```

- Обучите радиальную модель с несколькими значениями гамма
- Постройте график с предсказанными значениями
- Обучите радиальную модель с несколькими значениями epsilon
- Постройте график с предсказанными значениями
- Обучите полиномиальную модель с несколькими значениями степени полинома
- Постройте график с предсказанными значениями