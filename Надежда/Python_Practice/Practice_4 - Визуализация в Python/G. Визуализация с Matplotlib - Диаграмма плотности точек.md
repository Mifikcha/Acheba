
|   |   |
|---|---|
|Ограничение времени|1 секунда|
|Ограничение памяти|64Mb|
|Ввод|стандартный ввод или input.txt|
|Вывод|стандартный вывод или output.txt|

Используйте диаграмму плотности точек для отображения 500 случайных точек, распределенных по нормальному распределению. Выполните следующие действия:

1. Сгенерируйте 500 случайных точек для xx и yy из нормального распределения со средним значением μ=0μ=0 и стандартным отклонением σ=1σ=1.
2. Постройте диаграмму плотности, используя метод `hexbin`, чтобы отобразить плотность точек с использованием цветовой карты `plasma`.
3. Добавьте цветовую шкалу (`colorbar`) с меткой "Плотность", чтобы обозначить плотность точек на графике.
4. Установите заголовок графика "Диаграмма плотности точек", а также подписи осей: "Значение x" и "Значение y".

## Примечания

Используйте следующие импорты:

```
import matplotlib.pyplot as plt
import numpy as np
```

для визуализации и генерации данных соответственно.