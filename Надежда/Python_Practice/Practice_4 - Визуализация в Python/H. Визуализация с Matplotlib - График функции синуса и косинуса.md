
|   |   |
|---|---|
|Ограничение времени|1 секунда|
|Ограничение памяти|64Mb|
|Ввод|стандартный ввод или input.txt|
|Вывод|стандартный вывод или output.txt|

Постройте графики функций синуса и косинуса на одном графике для значений x от 0 до 2π.

Постройте графики функций:

- y=sin(x)y=sin(x)
- y=cos(x)y=cos(x)

Задайте разные стили линий для каждого графика:

- Для y=sin(x) синяя сплошная линия.
- Для y=cos(x) красная штриховая линия.

Добавьте легенду с надписями "y = sin(x)" и "y = cos(x)" для каждой функции.

Установите заголовок графика "График синуса и косинуса", подписи осей "Значение x" и "Значение y", и включите сетку.

## Примечания

Используйте следующие импорты:

```
import matplotlib.pyplot as plt
import numpy as np
```

для визуализации и генерации данных соответственно.