
|   |   |
|---|---|
|Ограничение времени|1 секунда|
|Ограничение памяти|64Mb|
|Ввод|стандартный ввод или input.txt|
|Вывод|стандартный вывод или output.txt|

Постройте полярный график функции r=1+cos(θ) для значений угла θ от 0 до 2π.

1. Определите значения угла θ в диапазоне от 0 до 2π.
    
2. Постройте график функции r=1+cos(θ) на полярной сетке.
    
3. Задайте цвет линии синим и установите её толщину равной 2.
    
4. Установите заголовок "Полярный график функции r = 1 + cos(θ)".
    

## Примечания

Используйте следующие импорты:

```
import matplotlib.pyplot as plt
import numpy as np
```

для визуализации и генерации данных соответственно.

Символ θ можете скопировать прямо из этой строки.