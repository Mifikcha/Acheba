
|   |   |
|---|---|
|Ограничение времени|1 секунда|
|Ограничение памяти|64Mb|
|Ввод|стандартный ввод или input.txt|
|Вывод|стандартный вывод или output.txt|

Рассчитайте средний ВВП по всем странам и добавьте его как новый столбец в таблицу.

Очистите данные в столбце GDP, удалив спецсимволы, такие как `$` и запятые, и преобразуйте его значения в числовой формат (float).

Рассчитайте средний ВВП по всем странам и добавьте его как новый столбец "Average GDP", который будет содержать одно и то же значение среднего ВВП для каждой строки.

Сохраните таблицу с этим столбцом в файл `countries_with_avg_gdp.csv`.

К файлу-источнику можно обратиться по имени `world-data-2023.csv`.

Источник данных - [https://www.kaggle.com/datasets/nelgiriyewithana/countries-of-the-world-2023](https://www.kaggle.com/datasets/nelgiriyewithana/countries-of-the-world-2023)

## Примечания

Необходимо использовать библиотеку **pandas**.

Файл-источник и файл с результатом должны находиться в одной директории с исполняемым скриптом.

[Скачать условие задачи](https://official.contest.yandex.ru/contest/71200/download/D/)